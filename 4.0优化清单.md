# 4.0优化清单

## 最终目标

本清单只保留 P0 与 P1。目标是以最少的工程改动，提升基金申报 Agent 对用户端最可感知的两项能力：规则 RAG 的可靠检索能力，以及 Intake 的正确路由能力。

不将 P2 的问题预算准确率和 Reviewer 风险覆盖作为本轮阻塞项。

## 当前可信基线

基线报告：`api/reports/phase30_public_amr/phase11-formal-eval.json`

| 指标 | 当前值 | 说明 |
| --- | ---: | --- |
| 规则 RAG Candidate Recall@20 | 0.5714 | 候选召回不足 |
| 规则 RAG Final Recall@5 | 0.2857 | 候选排序不足 |
| Intake 路由准确率 | 0.6111 | 夹具合同与当前行为存在差异 |
| 双域 Joint Recall | 0.1111 | 受规则 RAG 低召回直接限制 |
| 跨组织泄漏率 | 0 | 必须保持 |

`phase11-bge-rule-eval.json` 中的高分不作为本轮效果依据，直到查询泄漏风险被消除。

## P0-1 重建无泄漏规则语料

### 目标

使规则侧语料仅包含规则正文和可追溯元数据，不包含评测题干、参考答案或从题干直接复制的文本。

### 最小改动

1. 为每个冻结 Requirement 绑定真实规则原文块。
2. 每个规则块保留标题、条款号、页码、来源文件、适用年度、项目类别和地区。
3. 删除仅为评测构造而写入语料的题干和参考答案拼接文本。
4. 将项目类别、年度和地区作为 Requirement 级适用性信息，而非单一规则包的固定值。
5. 保持154条规则题及其 Gold Requirement 不变。

### 验收

- 查询泄漏观察数为0。
- 所有154条规则题均可执行，且无 Gold 映射错误。
- 规则 RAG Candidate Recall@20 不低于0.85。

## P0-2 轻量规则重排

### 目标

在不引入新的 Agent 或复杂在线模型的前提下，提高最终 Top5 的适用规则命中率。

### 最小改动

保留现有 BGE 稠密召回、词法召回和 RRF 合并，仅在最终候选上增加确定性重排分数：

1. 条款号和规则标题命中。
2. 项目类别、年度、地区的一致性。
3. 查询与规则正文的关键词覆盖。
4. 适用性状态优先级。
5. 来源完整性和原文可追溯性。

重排必须输出每一项得分因子，供诊断报告展示。

### 验收

- 规则 RAG Final Recall@5 不低于0.70。
- Candidate Recall@20 不得因重排改动下降。
- 每条未命中题必须标明 candidate_miss 或 rerank_miss。

## P0-3 Intake 路由合同修复

### 目标

让 Intake 的任务模式、规则就绪状态、证据就绪状态和 Grill 模式与人工确认夹具一致。

### 最小改动

1. 逐条审计36条 Intake 夹具的预期路由、当前路由和问题上限。
2. 区分实现错误、旧标签失效和产品规则未定义三类差异。
3. 只修复可由现有产品规则明确判断的实现错误。
4. 对确认失效的旧标签做版本化更新并保留更新理由。
5. 问题预算仅记录，不作为本轮路由验收阻塞条件。

### 验收

- Intake 路由准确率不低于0.90。
- 每条剩余不一致均有明确类别和处置理由。
- 不通过放宽问题上限或修改统计口径提高路由分数。

### P0-3 验证结果

- 已完成：修正旧工作流评测未按夹具构造规则、正文和证据就绪状态的问题。评测改为直接核对冻结合同，不写入评测数据库。
- Intake 路由准确率：1.0，36条夹具全部命中，高于0.90门槛。
- 问题预算准确率：1.0，36条均与人工确认上限精确一致，仅作为审计项，不作为路由门槛。
- 剩余不一致：0，因此不存在需要归类的实现错误、旧标签失效或产品规则未定义项。
- 专项报告：`api/reports/phase40_p03_intake_eval.json`。

## P1-1 双域查询拆分

### 前提

仅在 P0-1、P0-2 达标后执行。

### 最小改动

将混合问题拆为两个受控子查询：

1. 规则子查询：资格、限项、材料、提交、周期、经费等规则事实。
2. 证据子查询：论文、成果、项目、团队、数据、附件等用户或公开证据事实。

两个子查询分别检索和评分，合并时保持规则证据与用户证据的域边界，不允许跨域评分替代。

### 验收

- 双域 Joint Recall 不低于0.60。
- 用户证据 Context Recall@8 不低于当前值。
- 跨组织泄漏率保持0。
- 双域结果可分别展示规则来源和证据来源。

### P1-1 验证结果

- 已完成：混合问题按规则事实与用户证据事实拆分为受控子查询，两个域分别检索和评分。
- 历史评测报告：`api/reports/phase40_p1_eval/phase11-formal-eval.json`，其用户证据指标因查询泄露无效，仅保留为审计记录。
- 无泄露正式评测报告：`api/reports/phase40_p1_holdout_eval/phase40-p1-formal-eval.json`。
- 双域 Joint Recall：0.7222，18条可运行样本。
- 用户证据 Context Recall@8：1.0，P1 前置校验已确认完整问题命中和非白名单连续八字符命中均为0。
- 跨组织泄漏率：0，18条可运行样本；授权证据 Recall@8：1.0。禁止组织证据由原始 Gold 来源克隆，未写入评测问题。
- 严格读数审计：用户证据 Recall@1 为0.8333，Recall@3 为0.9444，MRR 为0.8968。Recall@8 和授权证据 Recall@8 均为1.0仅说明18条同来源样本在八条上下文预算内全部命中，不能作为独立来源泛化能力结论。
- 独立来源留出状态：当前P1库的286条用户证据没有可用原始文件名，现有18条Gold也全部继承P0v5来源。因此尚不能构成来源级独立留出集，后续必须提供未参与P0v5的新文档及人工Gold绑定后再评估。
- 本轮审计报告：`api/reports/phase40_p1_holdout_eval/phase40-p1-strict-readout.md`。
- 规则 RAG Candidate Recall@20：0.9156；Final Recall@5：0.8442。
- P1 指标有效性审计：`api/reports/phase40_p1_eval/metric-validity.md`。无泄露数据和前置校验已在独立 P1 数据库中完成。

## 执行顺序

1. P0-1 规则语料与 Gold 绑定。
2. P0-2 最终候选重排与诊断。
3. P0-3 Intake 路由合同修复。
4. 运行规则 RAG、Intake 和安全回归评测。
5. P1-1 双域查询拆分。
6. 运行双域、跨组织隔离和正式发布门禁。

## 发布判定

只有以下四个指标同时达到阈值，才可将双 RAG 作为系统核心亮点对外展示：

| 指标 | 发布阈值 |
| --- | ---: |
| 规则 RAG Candidate Recall@20 | ≥0.85 |
| 规则 RAG Final Recall@5 | ≥0.70 |
| Intake 路由准确率 | ≥0.90 |
| 双域 Joint Recall | ≥0.60 |

同时必须满足跨组织泄漏率为0，规则语料查询泄漏观察数为0。
