# 基金申报 Agent 3.0 优化方案

## 1. 方案定位

本方案承接 2.0 双 RAG、规则包、自适应 Intake、Claim 与 Reviewer 闭环，不重建 Agent 框架，不更换正式 Embedding 模型，不以扩大 Top K 掩盖问题。

本轮目标是先让评测与诊断可信，再分别提升规则检索、用户证据检索、双域协同和 Reviewer 风险识别能力。所有改动必须保持组织隔离、Proposal 隔离、规则包发布状态、来源追溯和人工决策保护。

正式环境固定使用现有 bge-base-zh-v1.5。Hash Embedding 仅用于 Stub 和确定性单元测试，禁止再作为语义 Recall 的正式评测后端。

## 2. 当前事实与总体判断

当前不是一个统一的 Recall 过低问题，而是评测合同、检索基础、双域调用和 Reviewer 能力四类问题叠加。

| 模块 | 当前报告值 | 已核验结论 | 是否可直接作为优化依据 |
| --- | ---: | --- | --- |
| Rule RAG | Recall@1 0.1364，Recall@5 0.4935 | 当前正式评测使用32维 Hash 向量，且候选语料含测试题干 | 否，只能作为流程基线 |
| 用户证据 RAG | Recall@5 0.75，Recall@20 0.925 | 服务最多展示8条结果，所谓 Recall@20 实际不是20条候选上的 Recall | 部分可参考 |
| 双域联合 | Joint Recall@5 0 | mixed 评测漏传 proposal_id，24题证据均被权限过滤 | 否，属于评测器缺陷 |
| 无适用规则 | 0.6111 | 14个案例返回规则包未发布，安全阻断被计为失败 | 否，需拆分状态 |
| 跨组织泄漏 | 0 | 24个可运行案例均未返回禁止证据 | 是，作为回归门槛 |
| 引用可追溯率 | 1.0 | 当前是来源字段存在率，不是引用支持率 | 否，需更名并补测 |
| Reviewer | 仅 duplicate_claim 与 missing_claim_evidence | 其余人工确认风险没有检测器实现 | 是，属于能力缺口 |
| Review Grill | 7个会话，3个首轮决策 | 会话是由任意章节问题触发，不等于12类风险已识别 | 仅可作流程观察 |

因此，本方案采纳总体方案的以下核心观点：

1. Rule RAG 必须区分候选召回与最终排序。
2. 用户证据检索应利用实体、数值、时间、状态和授权范围进行定向排序。
3. 双域问题必须独立构造规则查询与事实查询，并保存两路结果。
4. Reviewer 需要统一 Claim Ledger 和确定性检测器，不能依赖更长 Prompt。
5. Review Grill 必须是受控处置和复核链路，而不是任意回答后关闭问题。

同时，本方案不直接采纳以下判断：

1. 不以当前 Rule Recall 推断单纯的排序不足，因为当前后端不是 BGE，且夹具存在题干泄漏。
2. 不以当前用户证据 Recall@20 推断已有稳定的前20候选召回，因为实际展示上限为8。
3. 不以双域联合 Recall 为0推断 Router 或 State 覆盖问题，因为已定位到 proposal_id 缺失这一确定性原因。
4. 不以首轮决策记录率0.429推断全部 Grill 落库失败，因为现有执行器没有按预期风险码独立物化和断言。
5. 不在诊断完成前引入 Cross Encoder、模型微调或大规模重排服务。

## 3. 优化总原则

### 3.1 先评测，后调优

每一项检索优化前必须能够回答以下问题：

- Gold 是否已经入库并具有稳定外部 ID。
- Gold 是否被 Metadata、组织、Proposal、年度或发布状态过滤。
- Gold 是否进入候选池。
- Gold 是否进入最终提供给 Agent 的上下文。
- 失败属于数据、过滤、召回、排序、适用性判断还是标注问题。

无法归因的 Recall 不得用于模型、Chunk 或参数决策。

### 3.2 冻结测试与开发数据分离

Phase 11 已人工确认数据保持冻结。新增开发样本、别名、Hard Negative、查询改写和调参结果不得回写到冻结测试集。

规则语料和用户证据语料不得直接复制测试题干。候选资源应来自原始规则来源、脱敏材料摘要和结构化事实，查询应独立于候选文本。

### 3.3 权限和可追溯性不得退化

任何优化必须保持以下门槛：

- 跨组织泄漏率必须为0。
- Proposal 范围证据不得因召回优化而越权返回。
- 未发布规则包必须继续阻断，而不是被相似规则替代。
- 返回的规则、证据和 Reviewer Issue 必须保留原始来源和外部 ID。
- 锁定 Claim 与人工确认的 ProposalDecision 不得被自动覆盖。

## 4. 阶段 0：正式评测合同修复

状态：未开始。该阶段不改变线上检索排序，只修复指标定义和诊断数据。

### 4.1 修复事项

1. mixed 双域评测传入 proposal_id。

   当前正式评测仅以 organization_id 调用用户证据检索，而所有 mixed Gold Evidence 都绑定具体 Proposal。修复后分别报告规则侧命中、证据侧命中和联合命中。

2. 规则负例拆分状态。

   将 no_applicable_rule、grant_pack_not_published、grant_pack_required 和真实错误返回分开统计。未发布规则包的安全阻断不得计为无适用规则错误。

3. 拆分候选指标与上下文指标。

   规则侧报告 Candidate Recall@20、@40、Final Recall@1、@3、@5。用户证据侧报告 Candidate Recall@20、Context Recall@1、@3、@5、@8。禁止继续标注无法实际返回的 Recall@20。

4. 记录每条 Case 的完整诊断。

   每条记录至少保存 route、sub_queries、gold_ids、candidate_ids、candidate_scores、final_ids、gold_rank、applied_filters、filter_before_count、filter_after_count 和 failure_reason。

5. 更正引用指标名称。

   现有 citation_traceability_rate 改为 source_field_completeness_rate。另行建立 citation_entailment_rate，只对 Claim 与引用证据的支持关系计算。

6. 记录运行环境。

   每份正式报告必须写入 Embedding 后端、模型名、维度、索引版本、语料哈希、夹具哈希和命令版本。

### 4.2 失败原因枚举

| failure_reason | 含义 | 后续动作 |
| --- | --- | --- |
| ingestion_missing | Gold 不在索引或映射丢失 | 修复导入或外部 ID 映射 |
| metadata_filtered | Gold 被组织、Proposal、年度或发布状态过滤 | 修复查询合同或标注范围 |
| chunk_mismatch | Gold 粒度与可检索单元不一致 | 原子化规则或补邻接上下文 |
| candidate_miss | Gold 未进入候选池 | 改进检索与索引 |
| rerank_miss | Gold 在候选池但未进入最终上下文 | 改进排序 |
| applicability_error | 返回规则与适用条件不一致 | 改进适用性核验 |
| gold_mapping_error | 人工标注或外部映射不一致 | 回到人工复核 |

### 4.3 阶段验收

- [ ] mixed 的规则、证据和联合指标都具有独立分母。
- [ ] 不再出现将8条展示结果标为 Recall@20 的报告。
- [ ] 未发布规则包阻断与无适用规则拒答分开统计。
- [ ] 每个失败 Case 都能落入一种 failure_reason。
- [ ] 报告可复现同一次运行的模型、索引和夹具版本。

## 5. 阶段 1：可信 BGE 索引与独立语料

状态：未开始。该阶段建立真正可比较的语义基线。

### 5.1 统一嵌入合同

正式检索查询和候选 Chunk 必须同时使用 bge-base-zh-v1.5，并保证：

- 模型名与维度一致。
- 文档侧与查询侧使用一致的领域提示构造策略。
- 不允许32维 Hash 向量与768维 BGE 向量混合计算余弦相似度。
- 规则包和用户资料使用独立 Namespace，但共享同一 Embedding 服务。

### 5.2 重建评测候选语料

规则候选资源只包含：

- 原始条款或经人工确认的原子规则文本。
- 来源文档、页码、章节、年度、项目类别和适用范围。
- 条款编号、同义表达和否定或例外条件。

用户证据候选资源只包含：

- 脱敏材料摘要。
- EvidenceFact 的主体、角色、数值、时间、状态和授权范围。
- 资源类型、所属组织和 Proposal。

测试查询、参考答案和人工标注说明不得再写入候选正文。

### 5.3 中文词法基础修复

现有词法检索对中文整句切分不足。先引入确定性的中文特征，不引入新的大模型：

- 汉字二元与三元 gram。
- 年份、金额、比例、年龄、篇数和单位归一化。
- 规则编号和附件编号精确匹配。
- 基金领域别名表。
- 否定条件和例外条件标记。

### 5.4 阶段验收

- [ ] 正式报告明确显示 BGE 768维，而非 Hash 32维。
- [ ] 候选语料中不存在测试题干全文。
- [ ] 所有评测候选的 Embedding 模型和维度一致。
- [ ] 建立独立开发集与冻结测试集的来源级隔离。

## 6. 阶段 2：Rule RAG 候选召回、排序与适用性

状态：未开始。规则侧先解决可检索粒度和可解释诊断，再调整排序。

### 6.1 规则原子化

规则索引的最小单元应是一条可独立判定的规则，而不是固定长度段落。每条原子规则至少具备：

- 适用对象。
- 触发条件。
- 必须执行动作。
- 禁止事项。
- 例外条件。
- 时间范围。
- 来源章节和页码。
- 父章节与相邻上下文引用。

索引以 Atomic Rule 为单位，返回时携带 Parent Section，兼顾精确命中和上下文完整性。

### 6.2 规则候选流程

在 Phase 0 确认 candidate_miss 与 rerank_miss 的占比后，采用以下渐进式流程：

```text
规则问题
→ 规则 Metadata 过滤
→ BGE Dense Candidate
→ 中文词法与规则编号 Candidate
→ 去重与 RRF 合并
→ Candidate Top 40
→ 轻量规则排序
→ Final Top 5
```

轻量排序优先使用可解释特征：项目类别、年度、申请主体、阶段、章节、数值、否定条件、附件和条款编号。只有在 Candidate Recall 已达标但 Final Recall 仍不足时，才评估是否引入受控 Reranker。

### 6.3 适用性核验

检索相似不等于规则适用。对候选规则单独输出：

```text
applicable
not_applicable
insufficient_information
grant_pack_not_published
```

核验输入包括用户事实、规则条件、项目类别、申请阶段、时间范围和明确排除条件。未发布规则包优先返回阻断状态，不得用其他相似规则替代。

### 6.4 阶段验收

- [ ] Rule Candidate Recall@20、@40 可计算并按规则包分桶。
- [ ] 每条 Rule 失败可区分 candidate_miss 与 rerank_miss。
- [ ] 原子规则具有来源、父章节和适用范围。
- [ ] 无适用、信息不足和未发布阻断三种状态可区分。
- [ ] 跨组织泄漏率与来源字段完整率不退化。

## 7. 阶段 3：User Evidence RAG 定向排序

状态：未开始。该阶段不预设用户证据只需重排，先依据 Phase 0 的真实 Candidate Recall 决定范围。

### 7.1 先做范围和事实过滤

用户证据检索必须先固定：

- organization_id。
- proposal_id。
- owner_id 与授权范围。
- 证据类型。
- 时间范围。
- 章节和 Claim 意图。

不得为了提高 Recall 放宽组织或 Proposal 过滤。

### 7.2 排序特征

若 Gold 已进入候选池但未进入 Final Context，则在现有候选集上增加：

- 实体匹配。
- 数值与单位匹配。
- 时间和项目周期匹配。
- 人员角色匹配。
- 完成状态匹配。
- EvidenceFact 验证状态。
- 证据类型与章节匹配。
- 来源权威性与授权范围。

先使用确定性加分和可解释排序。只有排序损失仍明显时，再使用轻量 Reranker。

### 7.3 上下文组织

命中一个证据 Chunk 后，可在不扩大授权范围的前提下按需补充：

- 同一资源的相邻 Chunk。
- 表格表头与同一行。
- 附件摘要对应的原始页码。
- 实体别名和全称。

补充内容必须作为邻接证据标记，不能伪装为原始命中。

### 7.4 阶段验收

- [ ] Candidate Recall 与 Context Recall 分开报告。
- [ ] 用户证据 Final Context 不超过既定 token 预算。
- [ ] 组织、Proposal 与 owner_private 授权测试全部通过。
- [ ] 证据结果可解释其实体、数值、状态或类型匹配原因。

## 8. 阶段 4：双域检索协调器

状态：未开始。先修复确定性的 proposal_id 传递，再优化路由和配额。

### 8.1 双域合同

每次双域问题必须显式保留：

```text
normative_query
factual_query
rule_results
evidence_results
rule_candidate_ids
evidence_candidate_ids
rule_final_ids
evidence_final_ids
```

对 dual_domain，不得因任一路置信度较低而省略另一路检索。两路分数不得直接比较或混排。

### 8.2 固定上下文配额

默认组合由两类上下文构成：

- Rule Context 2条。
- Evidence Context 2条。
- Flexible Context 1条。

实际数量可按任务调节，但规则和用户事实必须保持独立容器，Writer 和 Reviewer 不得混淆两类证据的语义职责。

### 8.3 阶段验收

- [ ] 双域评测明确传递 proposal_id。
- [ ] 双域报告分别显示 Rule Recall、Evidence Recall 与 Joint Recall。
- [ ] 两路检索结果都写入 Retrieval Trace。
- [ ] 双域上下文按领域配额组装，不共享无类型 Top K。

## 9. 阶段 5：Claim Ledger 与 Reviewer 检测器

状态：未开始。Reviewer 的核心问题不是 Prompt 长度，而是可比较的数据结构和检测器缺失。

### 9.1 最小 Claim Ledger

在现有 Claim、EvidenceFact、ProposalDecisionLedger 基础上补齐可核验断言：

```text
claim_id
section_key
subject
predicate
object
normalized_value
unit
time_scope
polarity
completion_status
actor
role
funding_source
funding_amount
metric_name
metric_definition
evidence_fact_refs
attachment_refs
lock_state
human_decision_id
```

结构化字段只记录可验证断言，不替代原始 Claim 文本。

### 9.2 检测器实施顺序

第一批为确定性较强的检测器：

1. numerical_conflict。
2. completion_status_conflict。
3. duplicate_funding_conflict。
4. missing_attachment_evidence。
5. locked_claim_change_attempt。
6. human_decision_preserved。
7. missing_budget_decision。

第二批为需要结构化加语义判断的检测器：

1. role_attribution_conflict。
2. metric_definition_conflict。
3. cross_section_consistency。
4. unsupported_claim。
5. overclaiming。

必须明确区分：

- missing_claim_evidence 表示没有绑定用户证据。
- unsupported_claim 表示已绑定证据但不足以支持 Claim。
- numerical_conflict 表示同一主体、指标和时间范围出现冲突数值。
- overclaiming 表示证据仅支持较弱或较小范围结论，而 Claim 使用更强结论。

### 9.3 Reviewer 执行模式

```text
Claim Extraction
→ Issue Candidate Detection
→ 按 Issue 构造 Rule Query、Evidence Query 或 Cross Section Query
→ Issue Verification
→ 结构化 ReviewIssue
```

数值、角色、状态和锁定类风险优先通过结构化比较处理，不交给自然语言模型猜测。

### 9.4 阶段验收

- [ ] 夹具中的 role、status 和 decision 不再降级为 interpretive。
- [ ] 每条 Issue 都带受影响 Claim、EvidenceFact、规则或人工决策来源。
- [ ] 每个检测器独立报告 Precision、Recall 和误报率。
- [ ] 跨章节冲突不再只依赖文本完全重复。

## 10. 阶段 6：Review Grill 的受控处置与复核

状态：未开始。Review Grill 只处理已验证的高风险 Issue，不承担识别器职责。

每种 Issue 定义允许动作，例如：

- 补充证据。
- 缩小结论范围。
- 采用权威数值。
- 标记为待核验。
- 删除不受支持断言。
- 保留锁定事实。
- 进入人工确认。

用户回答后不得无条件关闭问题。系统必须重新核验，只有满足对应条件才将 Issue 标记为 resolved。

### 10.1 执行器修复

评测执行器必须按单个预期风险独立物化数据，并断言：

- 实际 Issue Code 与期望值一致。
- 受影响 Claim 一致。
- 问题卡对应正确 Issue。
- 允许动作与夹具期望处置一致。
- 决策持久化后重新运行 Reviewer 的结果正确。

不得再用同章节出现任意问题替代目标风险已被识别。

### 10.2 阶段验收

- [ ] Review Grill 仅由已验证高风险 Issue 创建。
- [ ] 每个决策都有幂等键、Decision、ReviewDecision 和审计记录。
- [ ] 非法处置不能关闭 Issue。
- [ ] 处置后必须重跑对应检测器。

## 11. 阶段 7：数据集、门槛与发布规则

### 11.1 数据量要求

每类 Reviewer 风险在开发集至少拥有：

- 5条正例。
- 5条近似负例。
- 3条边界例。

正式冻结评测每类至少拥有：

- 10条正例。
- 10条近似负例。
- 5条边界例。

涉及锁定修改和人工决策的案例必须提供修订前文本、修订后文本、Decision Ledger 和预期拒绝或允许结果。

### 11.2 发布门槛

以下为第一轮候选门槛，必须在完成可信 BGE 基线后最终锁定，不允许用当前 Hash 分数直接验收：

| 指标 | 发布要求 |
| --- | --- |
| Rule Candidate Recall@40 | 先建立 BGE 基线，再按规则包分桶设门槛 |
| Rule Final Recall@5 | 比 BGE 冻结基线显著提升，且无规则包明显退化 |
| 用户证据 Context Recall@5 | 比 BGE 冻结基线提升，组织隔离不得退化 |
| 双域 Joint Recall@5 | 在修复 proposal_id 后建立基线，再设门槛 |
| 未发布规则包阻断 | 必须保持100百分比 |
| 跨组织泄漏率 | 必须保持0 |
| 来源字段完整率 | 必须保持100百分比 |
| Reviewer 检测器 | 每类分别报告，不以总平均掩盖缺失类别 |
| Review Grill 处置复核 | 已关闭 Issue 必须100百分比通过复核 |

### 11.3 Ragas 的使用边界

Ragas 可以用于生成开发集候选、查询改写候选和 Hard Negative 候选，但不能：

- 替代冻结测试集的人审标签。
- 将自动生成题干直接写入候选语料。
- 自动确认规则适用性、资助冲突、锁定事实或人工决策。
- 作为正式发布门槛的唯一来源。

## 12. 唯一推荐实施顺序

```text
阶段 0  修复正式评测合同和诊断日志
→ 阶段 1  重建独立候选语料并建立可信 BGE 基线
→ 阶段 2  Rule RAG 原子化、候选召回和适用性核验
→ 阶段 3  User Evidence RAG 范围过滤和定向排序
→ 阶段 4  双域查询拆分、独立结果和上下文配额
→ 阶段 5  Claim Ledger 与 Reviewer 第一批确定性检测器
→ 阶段 6  Review Grill 受控处置与复核
→ 阶段 7  扩充冻结评测、回归门槛和发布报告
```

最先开始的具体工作为阶段0。它能在不改变线上业务行为的前提下，修复双域0分、伪 Recall@20、规则阻断误计分和来源指标误命名，并为后续每一项检索和 Reviewer 优化提供可验证依据。
