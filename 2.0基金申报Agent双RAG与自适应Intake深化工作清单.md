# 基金申报 Agent 双 RAG、规则包、自适应 Intake 与证据闭环深化工作清单

## 1. 规划结论

当前项目已经完成基础业务闭环、Structured Output、单套 Dense RAG、章节级证据注入、Tool Calling、MCP、Grill Me、LangGraph、人在回路、三 Agent 边界和运行级评测。后续不再重建 Agent 框架，也不再尝试新的 Embedding 模型，而是在现有工程基础上完成一次面向真实基金申报业务的领域化重构。

本轮优化的核心不是继续增加 Agent 数量，也不是替换模型，而是建立四个职责清晰、前后依赖明确的子系统：

1. 基金规则包子系统
   负责回答当前基金要求写什么、申请条件是什么、章节结构如何组织、哪些内容属于硬约束、哪些材料适用于当前年度和项目类别。

2. 用户资料与事实证据子系统
   负责回答用户实际具备哪些论文、项目、专利、实验、平台、团队和预算依据，哪些事实可以用于当前申请，哪些内容缺少材料支持。

3. 自适应 Intake 与项目共识子系统
   负责识别用户当前任务、交付深度、规则准备度、内容成熟度和证据准备度；先通过双 RAG 自动消除事实问题，再针对尚未确定的规划决策执行 Discovery 或 Refinement Grill，最终形成用户确认的 ProposalBrief、DecisionLedger 和 WorkPolicy。

4. 项目级生成与核验子系统
   负责将已确认的项目共识、规则要求和用户事实连接为章节计划、Claim、正文、引用、核验结果、Review Grill 和人工决策。

当前单套 RAG 必须拆成两套领域检索链，但底层基础设施不重复建设。正式环境继续固定使用现有 bge-base-zh-v1.5，Hash Embedding 继续只用于 Stub 和单元测试。本轮所有效果提升必须来自数据边界、Metadata、Chunk 策略、Hybrid Retrieval、Query Rewrite、Reranking、证据建模和纠错流程，而不是来自更换 Embedding。

本轮最重要的技术主线为：

```text
基金要求材料
→ 规则包编译器
→ 可版本化基金规则包
→ 规则 RAG

用户个人材料
→ 用户资料解析与事实抽取
→ 用户证据库
→ 用户证据 RAG

用户任务目标 + 交付深度 + 双 RAG 已知信息
→ ProposalIntakeProfile
→ ProposalWorkPolicy
→ Discovery 或 Refinement Grill
→ 用户确认 ProposalBrief

Confirmed ProposalBrief + 规则 RAG + 用户证据 RAG
→ Requirement 与 Claim 规划
→ 证据绑定
→ 正文生成
→ 双侧核验
→ 必要时 Review Grill
→ 局部修订
→ 人工确认
```

项目完成后，核心展示点应从已有的多 Agent 工作流升级为：

> 基于双域 RAG、自适应项目共识构建与 Claim 级证据核验的基金申报系统。系统使用同一 Embedding 基础服务，对基金规则和用户资料采用独立的解析、索引、检索、证据、核验和评测配置；先通过 Intake 与依赖式 Grill 确定用户真正需要解决的决策，再通过 Requirement、Claim、User Evidence 建立可追溯写作闭环。

### 1.1 本轮深化优化的交付标准

最终交付物必须同时满足以下八个条件：

1. 两套 RAG 真实隔离
   规则材料与用户资料不得进入同一个无类型索引，不得仅依靠 Prompt 区分。两套检索必须具有独立 Namespace、Metadata、Query Rewrite、召回参数、排序规则和 Eval。

2. Embedding 基线稳定
   正式环境继续使用现有 bge-base-zh-v1.5，不新增 Embedding 选型、替换和对比实验。所有回归报告必须记录相同模型名称、维度和版本。

3. 规则包可执行
   规则包不能只是 PDF 集合或独立向量库。至少应包含来源文件、项目元数据、章节 Schema、原子要求、适用范围、版本、优先级和冲突状态。

4. 用户事实可核验
   用户资料不能只保存 Chunk。系统必须区分已完成事实、计划内容、用户角色、时间、数值、验证状态和可使用范围。

5. 生成结果可追溯
   每个重要 Claim 至少可以关联一条 Requirement 和零至多条 User Evidence。事实性 Claim 无用户证据时不得伪造引用，应进入 missing_evidence 或 human_review。

6. 改造效果可量化
   必须使用同一 Embedding、同一冻结语料和同一测试问题，对比当前单套 Dense RAG 与新双 RAG。重点比较跨域误召回、适用范围错误、Recall@K、无答案判断、Claim 支持率和规则覆盖率。

7. 现有工程能力不回退
   原有 Proposal 主链路、LangGraph、人在回路、Tool Calling、MCP、权限、导出和运行轨迹必须继续通过。新能力通过扩展既有 service、tool 和 Graph State 接入，不在新节点内复制业务逻辑。

8. 用户路径自适应
   用户只需要选择当前任务和交付深度，不需要理解八种组合模式。系统必须根据材料状态自动决定是否进入 Grill、使用哪种 Grill、是否保留已有框架、Reviewer 检查深度和最大修订次数，并在执行规划前让用户确认工作方案和 ProposalBrief。

### 1.2 锁定后的总体修改顺序

本节是本轮深化优化的唯一权威顺序。原工作清单中已经完成的阶段不再重复建设，只作为当前基线。新工作从领域边界冻结开始，先拆数据和检索，再构建自适应 Intake 与项目共识，最后接入 Claim、生成、Reviewer 和 Multi Agent。

统一进入边界：

> 当现有主链路、单套 RAG、MCP、LangGraph、人在回路、Multi Agent 和 Phase 10 自动评测仍能稳定通过时，冻结当前版本为 dual-rag-baseline，不再修改 Embedding 和底层 Agent 框架，正式进入双 RAG 与规则包深化阶段。

统一完成边界：

> 当规则包和用户资料可以独立上传、解析、索引、检索和评测，系统能够根据用户任务与材料状态生成 WorkPolicy，通过自适应 Grill 形成 Confirmed ProposalBrief，章节规划能够同时引用 Requirement 与 User Evidence，Writer 和 Reviewer 能区分规则约束与事实证据，且不存在跨组织、跨项目、跨年度和跨知识域泄漏时，本轮核心优化完成。

#### 1.2.1 后续唯一 To Do 总览

```text
阶段 0  冻结现有可运行基线
→ 阶段 1  双知识域边界与配置盘点
→ 阶段 2  双域数据模型与迁移
→ 阶段 3  基金规则包编译器
→ 阶段 4  双域解析、Chunk 与索引链路
→ 阶段 5A  基金规则 RAG
→ 阶段 5B  用户资料与事实证据 RAG
→ 阶段 6  Query Router 与双检索协调器
→ 阶段 7  自适应 Intake、规划前 Grill 与项目共识构建
→ 阶段 8  基于项目共识的 Requirement、Claim、Evidence 数据闭环
→ 阶段 9  章节生成、双侧 Reviewer、Review Grill 与局部修订
→ 阶段 10  Tool、MCP、LangGraph 与 Multi Agent 适配
→ 阶段 11  双 RAG、Grill 与生成闭环 Eval
→ 阶段 12  人在回路、前端向导和规则包管理
→ 阶段 13  求职演示与交付收敛
```

最不能颠倒的依赖关系：

```text
知识域边界
→ 数据模型
→ 解析和索引
→ 两套独立 RAG
→ Query Router
→ 自适应 Intake 与规划前 Grill
→ Confirmed ProposalBrief
→ Claim 级绑定
→ Writer 和 Reviewer
```

```text
规则包原始文件
→ 原子 Requirement
→ Section Schema
→ Rule RAG
→ Intake 自动消除规则事实问题
→ 章节规划
→ 规则覆盖检查
```

```text
用户资料原始文件
→ User Evidence
→ User Evidence RAG
→ Intake 自动消除用户事实问题
→ Claim 支持判断
→ 事实性正文
→ Citation Entailment
```

```text
Writer
→ Reviewer
→ 自动可修复问题进入 Reviser
→ 需要用户决策的问题进入 Review Grill
→ 局部 Reviser
```

执行规则：

- 阶段 5A 与阶段 5B 可以并行，其余阶段默认串行
- 不再进行任何 Embedding 模型替换、模型尺寸比较和向量维度实验
- 正式环境固定使用现有 bge-base-zh-v1.5，测试环境继续允许 Hash Embedding
- 规则 RAG 与用户证据 RAG 必须共享 Embedding 服务，但不能共享无类型索引、Metadata Schema 和检索配置
- 任何规则都必须保留来源、页码、年度、项目类别、适用范围和版本
- 任何用户事实都必须保留来源、主体、角色、时间、验证状态和授权范围
- Writer 不得把 Rule Evidence 当作用户事实，也不得把 User Evidence 当作基金要求
- Reviewer 必须分别执行规则侧核验、事实侧核验和跨系统一致性核验
- LangGraph 只编排新 service，不在 Graph Node 中重写检索、解析和规则逻辑
- Multi Agent 继续使用现有固定边界，不新增自由协商 Agent
- 首版只验证三类差异明显的基金包，不一次收集全部基金体系
- 用户是否有规则包、是否有思路和是否需要润色不建立八套后端流程，统一映射为 ProposalIntakeProfile 与 ProposalWorkPolicy
- 用户只直接选择 task_mode 与 quality_level，其余准备度由系统检测并允许用户修正
- 固定五轮规划 Grill 和固定三轮局部 Grill 不再作为完成条件，改为问题预算、阻断缺口和用户确认共同决定
- mattpocock/skills 仅作为 Skill 分层、单问题追问、推荐答案和决策记录的设计参考，不作为新的运行时框架或独立 Agent 依赖

#### 1.2.2 当前已有内容与复用结论

状态：当前基线已完成，需要冻结，不重建。

当前已有内容：

- [x] Proposal 创建、规划、分章节写作、修订、审批、定稿和导出主链路
- [x] ProposalPlan、SectionDraft、ReviewResult 和 WorkflowError 等 Structured Output
- [x] WorkflowRun、run_id、AIJob、AIMetric 和运行轨迹
- [x] AIResource、AIChunk、EvidenceUsage 和 PDF 稳定解析
- [x] 正式 bge-base-zh-v1.5 与测试 Hash Embedding
- [x] Dense Top 20 到 Top 5 的单套 RetrievalService
- [x] Writer evidence_context 注入和章节级证据面板
- [x] 150 道合成题、15 道人工锚点和现有 RAG 回归报告
- [x] 业务 service、受控 tool、幂等性、组织权限和跨工作区拒绝
- [x] MCP 文档 Tools、Resources 和文件资产边界
- [x] Grill Me 规划澄清和局部修订澄清，当前为固定轮次基础实现
- [x] LangGraph StateGraph、Checkpointer、人在回路和恢复
- [x] Planner、Writer、Reviewer 三 Agent 工具白名单和显式交接
- [x] 顺序工作流、单 Graph 和 Multi Agent 的同源自动评测

当前复用原则：

| 已有内容 | 处理方式 | 说明 |
| --- | --- | --- |
| bge-base-zh-v1.5 | 复用并锁定 | 正式环境唯一 Embedding，不再试新模型 |
| Hash Embedding | 复用 | 仅用于测试、Stub 和确定性回归 |
| AIResource | 复用并扩展 | 增加 knowledge_domain、pack_id 和资料授权范围 |
| AIChunk | 复用并扩展 | 增加 chunk_type、parent_chunk_id、domain_metadata 和索引状态 |
| EvidenceUsage | 复用并改写职责 | 保留为实际 Prompt 注入审计，不再承担全部证据语义 |
| PDF 解析服务 | 复用 | 底层文本提取、页码、SHA256 和错误码不重写 |
| RetrievalService | 拆分 | 重构为共享检索基础类和两套领域 Service |
| 单一向量索引 | 拆分 | 变为 grant_rule 与 user_evidence 两个 Namespace |
| Writer evidence_context | 拆分 | 变为 rule_context 与 user_evidence_context |
| search_service | 拆分 | 变为 search_grant_rules 与 search_user_evidence |
| MCP Server | 复用并新增资源 | 继续复用解析能力，新增规则包和用户证据资源 |
| LangGraph | 复用并扩展 State | 不重建 Graph，增加 Intake、Grill、双检索和 Claim 字段 |
| Planner Agent | 改写输入职责 | 先读取规则，再读取证据库存摘要，不直接混合检索 |
| Writer Agent | 改写 Prompt 和工具 | 强制区分规则约束和事实证据 |
| Reviewer Agent | 拆分核验器 | 规则检查、事实检查和跨章节检查分别执行 |
| 现有 RAG Eval | 拆分并迁移 | 标注为规则题、用户证据题和混合题 |
| 前端证据面板 | 拆分 | 增加规则依据、用户证据和 Claim 状态三个视图 |
| Prompt 版本系统 | 复用并新增模板 | 新增规则解析、事实抽取、双域 Query Rewrite 和 Reviewer 模板 |
| ToolInvocation | 复用 | 增加 knowledge_domain、pack_version 和 evidence_scope 审计字段 |
| Grill Me | 复用并重写策略 | 保留现有会话、恢复和 HITL 基础，固定轮次改为 Router、Profile、问题预算和完成条件 |

阶段退出条件：

- 当前代码分支、测试报告和 RAG 指标已经冻结为 baseline
- 所有新任务都能标记为复用、拆分、新增或重写
- 不存在继续选型 Embedding 的开放任务
- 现有主链路和自动评测可以一条命令重复执行

#### 1.2.3 阶段 1：双知识域边界与配置盘点

目标：在改数据库和检索前，冻结规则材料与用户资料的职责边界，形成一张可直接指导代码修改的配置清单。

直接依赖：阶段 0

##### 1-1 冻结知识域枚举

新增统一枚举：

```text
knowledge_domain
├── grant_rule
└── user_evidence
```

- [ ] grant_rule 只允许保存基金指南、申请模板、通知、FAQ、预算规则、评审标准、形式审查要求和组织补充规则
- [ ] user_evidence 只允许保存简历、论文、专利、项目、实验报告、数据结果、设备清单、团队材料和预算依据
- [ ] 当前申请产生的章节、Claim、草稿和核验结果不得写入上述两个原始知识域
- [ ] 无法识别知识域的文件进入 pending_classification，不直接索引
- [ ] 已分类文件修改知识域时必须重新解析、重新索引并保留审计记录

处理方式：新增。

##### 1-2 冻结三层系统边界

```text
规则包层
负责 Requirement、Section Schema、Eligibility、Budget、Review Criteria

用户证据层
负责 User Evidence、事实、角色、数值、时间、授权范围

项目编排层
负责 Proposal、Section Plan、Claim、Evidence Binding、Review Issue
```

- [ ] 明确哪些模型属于公共规则包，哪些模型属于组织或用户私有资料
- [ ] 明确哪些数据只能绑定 organization_id，哪些必须绑定 proposal_id
- [ ] 明确规则包可以跨 Proposal 复用，用户资料默认不可跨组织复用
- [ ] 明确项目编排层只保存引用关系和快照，不复制整份原始文档

处理方式：新增架构边界，复用现有 Proposal 和 Organization 模型。

##### 1-3 盘点当前单套 RAG 调用链

- [ ] 列出上传、解析、Chunk、Embedding、索引、检索、Prompt 注入和 EvidenceUsage 的全部入口
- [ ] 标记同步 API、Celery、Tool、MCP 和 LangGraph 是否调用同一个 RetrievalService
- [ ] 标记当前 source_type、evidence_purpose、organization_id 和 proposal_id 的真实使用位置
- [ ] 找出所有默认搜索全部 AIChunk 的代码路径
- [ ] 找出所有未验证 knowledge_domain 的 EvidenceUsage 写入路径

处理方式：复用代码盘点，后续重写无域过滤路径。

##### 1-4 冻结配置归属

需要形成两份独立配置：

| 配置项 | grant_rule | user_evidence |
| --- | --- | --- |
| Namespace | grant_rule | user_evidence |
| 主过滤键 | pack_id、year、program_type、region | organization_id、proposal_id、owner_id、authorization_scope |
| Chunk 目标 | 原子规则和完整适用条件 | 完整事实和可解释上下文 |
| Query Rewrite | 官方规则语言 | 用户事实和成果语言 |
| Dense 权重 | 辅助召回 | 主要语义召回 |
| BM25 权重 | 较高 | 中等 |
| Rerank 特征 | 年度、权威级别、适用范围 | 事实相关性、用户角色、时间、证据强度 |
| 无答案策略 | 不适用或需人工确认 | missing_evidence |
| 核心指标 | 适用性、版本正确率、Precision | Recall、Claim 支持率、角色正确率 |

处理方式：拆分。

##### 1-5 增加迁移开关

- [ ] 增加 DUAL_RAG_ENABLED
- [ ] 增加 RULE_RAG_HYBRID_ENABLED
- [ ] 增加 USER_RAG_HYBRID_ENABLED
- [ ] 增加 RERANK_ENABLED
- [ ] 增加 CLAIM_LEVEL_GROUNDING_ENABLED
- [ ] 所有开关默认在测试环境可独立启用
- [ ] 保留旧 RetrievalService 作为迁移期 baseline，不长期双写

处理方式：新增。

阶段退出条件：

- 两个知识域和项目编排层的边界形成书面定义
- 所有配置项都有明确归属
- 所有当前单套 RAG 调用入口已经定位
- 可以在不改 Embedding 的前提下开始数据迁移

#### 1.2.4 阶段 2：双域数据模型与迁移

目标：复用 AIResource 和 AIChunk 的稳定标识、解析与组织隔离能力，为规则包和用户证据增加类型安全的数据结构。

直接依赖：阶段 1

##### 2-1 扩展 AIResource

新增字段：

```text
knowledge_domain
classification_status
source_authority
source_effective_date
source_expiry_date
grant_pack_id
owner_user_id
authorization_scope
supersedes_resource_id
```

- [ ] knowledge_domain 必填，历史数据迁移期允许 unknown
- [ ] grant_rule 文件必须绑定 grant_pack_id 或处于 pack_draft 状态
- [ ] user_evidence 文件必须绑定 organization_id，并按需要绑定 proposal_id 或 owner_user_id
- [ ] authorization_scope 支持 organization、proposal、owner_private
- [ ] source_authority 只对 grant_rule 生效
- [ ] source_effective_date 和 source_expiry_date用于规则版本过滤

处理方式：复用并扩展。

##### 2-2 扩展 AIChunk

新增字段：

```text
knowledge_domain
chunk_type
parent_chunk_id
section_path
rule_number
entity_keys
index_namespace
index_version
```

- [ ] grant_rule 的 chunk_type 至少包含 requirement、eligibility、budget、review_criterion、section_instruction、submission_rule
- [ ] user_evidence 的 chunk_type 至少包含 publication、project、patent、experiment、dataset、equipment、team_profile、budget_basis
- [ ] parent_chunk_id 支持 Parent Child Retrieval
- [ ] section_path 保存完整标题路径
- [ ] index_namespace 与 knowledge_domain 强一致
- [ ] 相同文档版本和解析版本下 Chunk ID 继续稳定

处理方式：复用并扩展。

##### 2-3 新增规则包模型

新增核心实体：

```text
GrantProgram
GrantPack
GrantPackVersion
GrantPackDocument
GrantRequirement
EligibilityRule
SectionSchema
ReviewCriterion
BudgetRule
SubmissionRule
RuleConflict
RuleExtractionReview
```

最低字段要求：

- [ ] GrantProgram 保存基金体系、项目类别、地区和主管机构
- [ ] GrantPack 保存可选择的规则包标识
- [ ] GrantPackVersion 保存年度、版本、状态、发布日期和最后核验日期
- [ ] GrantRequirement 保存 requirement_type、mandatory、target_section、validation_method 和适用条件
- [ ] SectionSchema 保存章节 key、标题、顺序、必填状态、字数限制和子章节
- [ ] RuleConflict 保存冲突对象、来源优先级、冲突说明和人工结论
- [ ] 所有结构化规则必须关联原始 AIChunk 和页码

处理方式：新增。

##### 2-4 新增用户证据模型

新增核心实体：

```text
UserEvidence
EvidenceFact
EvidenceEntity
EvidenceVerification
EvidenceAuthorization
```

最低字段要求：

- [ ] UserEvidence 关联 AIChunk 和原始文件
- [ ] EvidenceFact 保存 subject、predicate、object、fact_type、time_range 和 numeric_value
- [ ] fact_status 区分 completed、ongoing、planned、uncertain
- [ ] user_role 区分负责人、第一作者、参与者、团队成员和未知
- [ ] verification_status 区分 extracted、user_confirmed、conflicted、rejected
- [ ] EvidenceAuthorization 控制是否可用于当前 Proposal
- [ ] 敏感材料可以只保存受控摘要和引用，不进入公共规则包

处理方式：新增。

##### 2-5 改写 EvidenceUsage 的职责

- [ ] EvidenceUsage 继续记录实际进入 Prompt 的 Chunk 快照
- [ ] 增加 evidence_domain、retrieval_run_id、rank、score、query_id 和 injection_role
- [ ] 不再使用 EvidenceUsage 表示 Requirement 或 User Evidence 的全部语义
- [ ] 新增 ClaimEvidenceBinding 保存 Claim 与 GrantRequirement、UserEvidence 的关系
- [ ] 历史 EvidenceUsage 迁移为 user_evidence 或 pending_review，不自动视为基金规则

处理方式：复用并改写职责。

##### 2-6 历史数据分类与迁移

- [ ] 对当前 14 份 PDF 和 249 个 Chunk 建立分类清单
- [ ] 指南、模板、审查规则迁移到 grant_rule
- [ ] 团队资料、项目材料迁移到 user_evidence
- [ ] 无法确定归属的资料进入 pending_classification
- [ ] 迁移前后 document_id 和 chunk_id 保持稳定
- [ ] 迁移不得重新计算 Embedding，除非 Chunk 内容或上下文发生变化
- [ ] 生成迁移报告，包含文件数、Chunk 数、未分类数和失败原因

处理方式：拆分并迁移。

阶段退出条件：

- AIResource 和 AIChunk 均具有明确 knowledge_domain
- 规则包、Requirement、User Evidence 和 Claim Binding 模型迁移通过
- 历史资料完成分类且无跨组织变化
- 现有 Embedding 向量可以在无内容变化时复用
- EvidenceUsage 只承担注入审计，不再承担领域语义

#### 1.2.5 阶段 3：基金规则包编译器

目标：将用户批量上传的基金申报要求材料编译为可审核、可版本化、可执行的规则包，而不是建立一个文件夹或普通知识库。

直接依赖：阶段 2、现有 MCP 文档服务

首版验证范围：

- [ ] 国家自然科学基金青年科学基金项目
- [ ] 一个省级自然科学基金项目
- [ ] 一个教改项目

三类规则包必须存在明显结构差异，用于验证抽象是否成立。

##### 3-1 批量上传与文件分类

- [ ] 一个规则包草稿支持批量绑定多个 file_id
- [ ] 复用 MCP parse_document、extract_document_structure 和 extract_requirements
- [ ] 新增文档类型分类：guide、template、notice、faq、budget、review、form_check、organization_supplement
- [ ] 自动识别基金体系、项目类别、年度、地区和主管机构
- [ ] 低置信度分类进入人工确认，不直接发布
- [ ] 相同 SHA256 文件不重复导入同一规则包版本

处理方式：复用 MCP，新增规则包批处理 service。

##### 3-2 原子 Requirement 提取

- [ ] 将长段指南拆为原子 Requirement
- [ ] 区分 mandatory、recommended、informational
- [ ] 区分 eligibility、section_content、format、budget、submission、review
- [ ] 保存 applicable_condition，不将条件句拆丢
- [ ] 保存 target_section 和 validation_method
- [ ] 保存来源原文、页码、规则编号和提取 Prompt 版本
- [ ] 提取结果必须通过 Structured Output 校验后落库

处理方式：新增 Prompt、Schema 和 service，复用现有 Prompt 版本治理。

##### 3-3 Section Schema 编译

- [ ] 从模板和指南提取章节层级
- [ ] 生成稳定 section_key
- [ ] 保存章节顺序、必填状态、字数限制和字段类型
- [ ] 将 Requirement 绑定到一个或多个章节
- [ ] 允许组织补充包覆盖章节提醒，但不得静默覆盖官方硬约束
- [ ] 章节结构变更必须形成新 GrantPackVersion

处理方式：新增。

##### 3-4 规则优先级与冲突检测

默认优先级：

```text
正式申报通知
> 官方项目指南
> 官方填报说明
> 官方 FAQ
> 依托单位内部通知
> 用户补充说明
```

- [ ] 优先级仅用于排序和提示，不自动删除冲突规则
- [ ] 相同规则类型、适用范围相同但值不同，创建 RuleConflict
- [ ] 冲突必须展示双方原文和来源
- [ ] 人工确认后保存 resolution 和 reviewer_id
- [ ] 未解决冲突的规则包不能进入 published

处理方式：新增。

##### 3-5 规则包状态与版本

```text
draft
→ extracted
→ needs_review
→ validated
→ published
→ superseded
→ archived
```

- [ ] 已发布规则包不可原地修改结构化规则
- [ ] 更新材料时创建新版本
- [ ] 历史 Proposal 继续绑定原版本，保证可复现
- [ ] 创建 Proposal 时必须显示规则包年度和版本
- [ ] 过期规则包默认不可创建新 Proposal

处理方式：新增。

##### 3-6 规则包编译测试

- [ ] 正常多文件包
- [ ] 缺少模板
- [ ] 文件年度冲突
- [ ] 项目类别冲突
- [ ] 重复文件
- [ ] 扫描件或损坏文件
- [ ] 提取结构非法
- [ ] 未解决规则冲突
- [ ] 发布后不可原地修改
- [ ] 历史版本仍可读取

阶段退出条件：

- 三个首版规则包均可从原始文件编译并发布
- 每条 Requirement 可追溯到文件、页码和原文
- Section Schema 可以直接供 Planner 使用
- 未解决冲突不会进入正式检索
- 规则包版本不可变且历史 Proposal 可复现

#### 1.2.6 阶段 4：双域解析、Chunk 与索引链路

目标：复用同一 PDF 解析和 Embedding 服务，对两个知识域建立独立的后处理、Chunk、上下文增强和索引流程。

直接依赖：阶段 2、阶段 3

##### 4-1 保留共享底层能力

以下内容不重写：

- [ ] 文件安全读取和 organization 校验
- [ ] PDF 文本提取、页码、错误码和 SHA256
- [ ] parser_version 和稳定 Chunk ID
- [ ] bge-base-zh-v1.5 Embedding 服务
- [ ] Hash Embedding 测试替身
- [ ] 向量存储客户端和批量写入机制
- [ ] AIJob、Celery 和运行日志

处理方式：复用。

##### 4-2 拆分规则材料 Chunker

规则 Chunk 原则：一条规则及其完整适用条件形成一个检索单元。

- [ ] 保留标题路径、条款编号、项目类别和年度
- [ ] 条件、例外和限制不得与主规则拆开
- [ ] 表格按规则行拆分，同时继承表头和脚注
- [ ] FAQ 问题和答案保持在同一个 Chunk
- [ ] 同义或重复规则不直接删除，标记 canonical_rule_id
- [ ] 生成 rule_context_prefix 后再计算 Embedding

初始 Chunk 配置：

```text
目标长度：180 至 450 tokens
最大长度：700 tokens
重叠：仅在跨页条款时使用
父节点：完整小节或完整表格
```

处理方式：重写领域后处理，复用底层解析。

##### 4-3 拆分用户资料 Chunker

用户资料 Chunk 原则：一个完整事实、成果或实验上下文形成一个检索单元。

- [ ] 论文条目保留题目、作者顺序、年份和期刊会议
- [ ] 项目条目保留项目名称、角色、周期、经费和结果
- [ ] 实验结果保留条件、指标、数值、结论和表图标题
- [ ] 简历和团队材料按完整经历拆分
- [ ] Parent Child Retrieval 返回完整段落或完整成果块
- [ ] 生成 evidence_context_prefix 后再计算 Embedding

初始 Chunk 配置：

```text
目标长度：300 至 700 tokens
最大长度：1200 tokens
重叠：60 至 120 tokens
父节点：完整成果、完整实验小节或完整项目经历
```

处理方式：重写领域后处理，复用底层解析。

##### 4-4 拆分索引 Namespace

- [ ] grant_rule 只写入规则索引
- [ ] user_evidence 只写入用户证据索引
- [ ] 同一 Chunk 不能同时存在于两个正式 Namespace
- [ ] 查询时必须显式提供 knowledge_domain
- [ ] 未提供 domain 的生产调用直接拒绝，不默认全库搜索
- [ ] 删除或更新文件时只影响所属 Namespace
- [ ] Namespace 操作记录 organization_id、pack_id 或 proposal_id

处理方式：拆分。

##### 4-5 Embedding 复用与重算规则

- [ ] 模型固定为现有 bge-base-zh-v1.5
- [ ] 不新增 Embedding Provider 抽象，不做模型动态路由
- [ ] 原 Chunk 文本未变化时复用现有向量
- [ ] 添加 Context Prefix、修改 Chunk 边界或修正文案时才重算
- [ ] 向量记录增加 embedding_input_hash
- [ ] 回归报告必须记录模型名、维度、归一化方式和索引版本

处理方式：复用并锁定。

##### 4-6 双域索引测试

- [ ] 同一查询不会跨 Namespace 返回结果
- [ ] 规则包查询不会返回用户简历或实验材料
- [ ] 用户证据查询不会返回基金指南
- [ ] 跨组织用户资料返回零结果
- [ ] 错误年度规则经 Metadata Filter 后不进入候选集
- [ ] 重新索引幂等
- [ ] Chunk 更新只重算受影响向量

阶段退出条件：

- 两套 Chunker 和两个 Namespace 可独立运行
- Embedding 模型没有变化
- 规则 Chunk 和用户资料 Chunk 的人工抽检通过
- 生产路径不存在无 domain 检索
- 跨域和跨组织泄漏测试全部通过

#### 1.2.7 阶段 5A：基金规则 RAG

目标：建立强调适用范围、年度、权威级别和精确条款的规则检索链。

直接依赖：阶段 4

并行关系：可与阶段 5B 同时进行

##### 5A-1 定义 RuleQuery

输入字段：

```text
pack_version_id
program_type
year
region
section_key
requirement_type
user_question
```

- [ ] pack_version_id 为硬过滤条件
- [ ] year、program_type 和 region 不允许由 Embedding 模糊匹配替代
- [ ] section_key 和 requirement_type 用于缩小候选范围
- [ ] 缺少当前规则包时返回 grant_pack_required

处理方式：新增 Schema，复用公共 WorkflowError。

##### 5A-2 规则 Query Rewrite

- [ ] 将自然语言问题改写为官方规则语言
- [ ] 保留基金名称、年度、项目类别、章节和规则类型
- [ ] 禁止生成不存在的条款编号
- [ ] 每次最多生成三条检索 Query
- [ ] 资格类和预算类 Query 禁止使用无约束 HyDE
- [ ] Rewrite 输出保存 Prompt 版本和 run_id

处理方式：新增领域 Prompt。

##### 5A-3 建立 Hybrid Retrieval

初始候选链：

```text
Metadata Hard Filter
→ BM25 Top 40
+ Dense Top 40
→ RRF 合并
→ Candidate Top 20
```

- [ ] BM25 索引覆盖条款编号、标题、年份、项目类别、金额和固定术语
- [ ] Dense 使用现有 bge-base-zh-v1.5
- [ ] RRF 参数配置化，不写死在 View
- [ ] 候选合并按 chunk_id 去重
- [ ] 所有候选必须属于当前 published GrantPackVersion

处理方式：新增 BM25 和 RRF，复用 Dense Retrieval。

##### 5A-4 规则感知 Rerank

排序特征：

```text
文本相关性
适用范围匹配
年度匹配
项目类别匹配
章节匹配
来源权威级别
规则状态
冲突状态惩罚
```

- [ ] Reranker 服务可以与用户资料侧共用模型
- [ ] 规则侧必须使用独立评分配置
- [ ] 冲突未解决的规则不得作为唯一答案
- [ ] 旧年度规则不得因语义相似进入 Top K
- [ ] 返回 Top 5，同时保留 Top 20 审计候选

处理方式：新增或接入 Reranker，拆分评分配置。

##### 5A-5 规则证据输出

每条结果至少包含：

```text
requirement_id
chunk_id
source_document
page_number
original_text
applicable_scope
authority_level
pack_version
conflict_status
retrieval_score
```

- [ ] 不直接返回模型总结替代原文
- [ ] 规则总结和原始条款分字段保存
- [ ] 无适用规则时返回 no_applicable_rule
- [ ] 存在冲突时返回 human_review_required

处理方式：新增 RuleEvidence Schema。

##### 5A-6 规则 RAG 测试

- [ ] 同名不同年度规则
- [ ] 青年与面上项目差异
- [ ] 省基金与国自然差异
- [ ] 教改与自然科学章节差异
- [ ] 条款编号精确检索
- [ ] 金额和字数限制检索
- [ ] FAQ 与正式指南冲突
- [ ] 无适用规则问题
- [ ] 未发布规则包拒绝检索

阶段退出条件：

- 规则检索只在当前规则包版本内执行
- 错误年度和错误项目类别不会进入 Top 5
- 每条结果可追溯到原文和页码
- no_applicable_rule 和冲突分支可稳定触发
- 规则 RAG 有独立回归报告

#### 1.2.8 阶段 5B：用户资料与事实证据 RAG

目标：建立强调高召回、用户角色、事实完整性、时间和证据强度的用户资料检索链。

直接依赖：阶段 4

并行关系：可与阶段 5A 同时进行

##### 5B-1 定义 EvidenceQuery

输入字段：

```text
organization_id
proposal_id
owner_id
section_key
claim_intent
evidence_types
time_range
user_question
```

- [ ] organization_id 为硬过滤条件
- [ ] proposal_id 控制当前项目专属资料
- [ ] owner_id 用于个人私有材料
- [ ] evidence_types 支持论文、项目、实验、专利、设备、团队和预算依据
- [ ] 未授权资料不能进入候选集

处理方式：新增 Schema，复用现有权限检查。

##### 5B-2 用户证据 Query Rewrite

- [ ] 将宽泛章节问题拆为具体事实检索问题
- [ ] 按 Claim 类型生成 Query，而不是无约束扩写
- [ ] 数值类 Query 保留指标、单位、条件和时间
- [ ] 角色类 Query 保留负责人、第一作者、参与者等限定词
- [ ] 每次最多生成五条 Query
- [ ] Query 与原始问题均写入检索审计

处理方式：新增领域 Prompt。

##### 5B-3 建立 Hybrid 与 Parent Child Retrieval

初始候选链：

```text
Permission Filter
→ Dense Top 40
+ BM25 Top 30
→ RRF 合并
→ Candidate Top 30
→ Parent Expansion
```

- [ ] Dense 继续使用现有 bge-base-zh-v1.5
- [ ] BM25 补充项目名、论文名、专利号、设备型号和数值
- [ ] Parent Expansion 只扩展命中的成果或实验上下文
- [ ] 不返回整本文档
- [ ] 合并候选按事实实体和 chunk_id 去重

处理方式：新增 BM25、RRF 和 Parent Child，复用 Dense Retrieval。

##### 5B-4 用户证据感知 Rerank

排序特征：

```text
Claim 语义相关性
用户角色匹配
时间有效性
事实完整性
直接证据强度
当前 Proposal 授权
数值可验证性
冲突惩罚
```

- [ ] planned 事实不得排在 completed 事实之前支撑已完成表述
- [ ] 用户仅为参与者的材料不得作为负责人能力的唯一强证据
- [ ] conflicted 或 rejected EvidenceFact 不进入正常 Top K
- [ ] 返回 Top 8，同时保留 Top 30 审计候选

处理方式：拆分评分配置。

##### 5B-5 证据充分性判断

- [ ] 判断 evidence_status 为 sufficient、partial、missing、conflicted
- [ ] numerical Claim 必须存在直接数值证据
- [ ] completed Claim 不允许只由 planned 证据支持
- [ ] comparative Claim 需要至少两个可比较来源或明确标记为分析判断
- [ ] 无证据时返回 missing_evidence，不生成虚假 evidence_id
- [ ] 部分证据时建议缩小表述或补充材料

处理方式：新增 Evidence Grader。

##### 5B-6 用户资料 RAG 测试

- [ ] 论文题目精确召回
- [ ] 跨文件同一项目聚合
- [ ] 用户角色识别
- [ ] 已完成与计划内容区分
- [ ] 数值和单位一致性
- [ ] 无证据问题
- [ ] 冲突材料
- [ ] 未授权资料拒绝
- [ ] 跨组织访问拒绝

阶段退出条件：

- 用户资料检索严格按组织、Proposal 和授权范围过滤
- Evidence Grader 可以稳定区分充分、部分、缺失和冲突
- 数值、角色和时间类错误有自动测试
- 用户资料 RAG 有独立回归报告

#### 1.2.9 阶段 6：Query Router 与双检索协调器

目标：在 Planner、Writer 和 Reviewer 之前判断任务需要规则检索、用户证据检索还是双检索，防止两个知识域被混合调用。

直接依赖：阶段 5A、阶段 5B

##### 6-1 定义 QueryIntent

```text
rule_only
user_evidence_only
dual_grounding
rule_validation
fact_validation
cross_validation
```

- [ ] 规则查询优先使用确定性关键词、当前界面和 tool 名判断
- [ ] 模糊请求再使用结构化 LLM Router
- [ ] Router 输出必须包含 intent、reason、required_domains 和 confidence
- [ ] 低置信度进入 dual_grounding 或 human_review，不静默选择错误域

处理方式：新增。

##### 6-2 新增双检索 Service

```text
search_grant_rules
search_user_evidence
retrieve_for_section
validate_claim_grounding
```

- [ ] search_grant_rules 只调用 RuleRAGService
- [ ] search_user_evidence 只调用 UserEvidenceRAGService
- [ ] retrieve_for_section 分别返回 rule_results 与 user_evidence_results
- [ ] 不允许将两个结果先拼成无类型 Chunk 列表
- [ ] 每次检索生成 retrieval_run_id

处理方式：拆分现有 search_service。

##### 6-3 定义双域结果协议

```text
DualRetrievalResult
├── rule_query
├── rule_results
├── evidence_query
├── user_evidence_results
├── requirement_gaps
├── evidence_gaps
└── conflicts
```

- [ ] 两个结果使用不同 Schema
- [ ] 每条结果保留 domain、rank 和原始分数
- [ ] 上层 Writer 不能根据文本猜测证据类型
- [ ] 结果快照进入 WorkflowRun 轨迹

处理方式：新增公共协议。

##### 6-4 路由权限

- [ ] Planner 可以检索规则和用户证据库存摘要，但不能读取未授权原文
- [ ] Writer 可以读取当前章节所需规则和已授权用户证据
- [ ] Reviewer 可以读取两侧证据和冲突状态
- [ ] Formatter 不主动检索
- [ ] 用户工具可以查看证据，但不能伪造 evidence_id

处理方式：改写现有工具白名单。

##### 6-5 路由测试

- [ ] 当前基金研究基础要求是什么，路由至 rule_only
- [ ] 我有哪些成果可以写，路由至 user_evidence_only
- [ ] 帮我规划研究基础，路由至 dual_grounding
- [ ] 这句话是否符合指南，路由至 rule_validation
- [ ] 这项数据有材料支持吗，路由至 fact_validation
- [ ] 模糊低置信度问题不会只检索一个域

##### 6-6 为阶段 7 输出可消费的缺口协议

新增：

```text
IntakeKnowledgeSnapshot
├── resolved_rule_facts
├── resolved_user_facts
├── unresolved_rule_gaps
├── unresolved_evidence_gaps
├── conflicts
└── source_trace
```

- [ ] 阶段 6 只回答系统已经知道什么和仍缺少什么，不替用户作出研究方向、创新主线和取舍决策
- [ ] resolved_rule_facts 和 resolved_user_facts 必须保留 evidence_id、来源和置信状态
- [ ] unresolved_rule_gaps、unresolved_evidence_gaps 和 conflicts 可以直接被阶段 7 的 Grill Router 消费
- [ ] 双检索结果不得直接转成 ProposalBrief，必须经过阶段 7 的决策确认

处理方式：新增阶段间协议。

阶段退出条件：

- 所有生产检索调用必须经过显式 domain 或 Query Router
- 双检索结果保持类型隔离
- Agent 越权和错误路由有自动测试
- 运行轨迹可以查看两次检索的独立耗时和结果
- 可以输出阶段 7 所需的 IntakeKnowledgeSnapshot，不把检索结果误当作用户决策

#### 1.2.10 阶段 7：自适应 Intake、规划前 Grill 与项目共识构建

目标：在正式创建章节和 Claim 之前，识别用户当前拥有什么、希望系统完成到什么程度以及哪些关键决策仍未确定。系统先使用双 RAG 自动消除可由材料回答的事实问题，再通过单问题、依赖式 Grill 构建用户确认的 ProposalBrief。本阶段是阶段 6 与阶段 8 之间的强制转换层，不直接写正文，也不提前创建最终 Claim。

直接依赖：阶段 1、阶段 2、阶段 3、阶段 5A、阶段 5B、阶段 6

复用基线：现有 Structured Output、Prompt 版本系统、Grill 会话持久化、HumanApprovalTask、Checkpointer 和权限体系

边界说明：

- mattpocock/skills 中 grill-me、grilling、grill-with-docs 和 domain-modeling 的价值在于 Skill 分层、每次只解决一个依赖问题、提供推荐答案、先查环境再问用户以及即时记录决策
- 首版只复用这些设计原则和可改写 Prompt，不引入新的运行时框架，不新增 Grill Agent，不让外部 Skill 直接访问数据库
- Discovery Grill 与 Refinement Grill 在本阶段正式实现
- Review Grill 只在本阶段冻结协议和 Profile，实际启用必须等待阶段 9 的 Reviewer 产生 ReviewIssue
- LangGraph Grill 子图在阶段 10 接入，正式前端向导和单问题界面在阶段 12 接入

##### 7-1 定义 ProposalIntakeProfile

新增项目级输入状态：

```text
ProposalIntakeProfile
├── rule_readiness
├── content_maturity
├── evidence_readiness
├── task_mode
├── quality_level
├── detected_inputs
└── user_overrides
```

枚举：

```text
rule_readiness
├── verified_pack
├── uploaded_pending
├── generic_fallback
└── missing_blocking
```

```text
content_maturity
├── none
├── rough_notes
├── outline
├── partial_draft
└── full_draft
```

```text
evidence_readiness
├── none
├── partial
├── sufficient
└── verified
```

```text
task_mode
├── polish_existing
├── refine_outline
└── plan_from_scratch
```

```text
quality_level
├── quick
├── standard
└── deep
```

- [ ] task_mode 与 quality_level 由用户主动选择
- [ ] rule_readiness、content_maturity 和 evidence_readiness 由系统根据材料检测生成
- [ ] 用户可以修正系统检测结果，但所有覆盖必须写入审计记录
- [ ] IntakeProfile 必须绑定 proposal_id、organization_id 和 schema_version
- [ ] 同一个 Proposal 可以在后续上传材料后重新评估，但不得覆盖历史确认快照

处理方式：新增模型和 Schema，复用 Proposal、Organization 和现有上传边界。

##### 7-2 定义 ProposalWorkPolicy

根据 IntakeProfile 和 IntakeKnowledgeSnapshot 生成执行策略：

```text
ProposalWorkPolicy
├── grill_mode
├── question_budget
├── preserve_user_structure
├── planning_mode
├── writing_mode
├── allowed_assumptions
├── enabled_reviewers
├── max_auto_revisions
└── blocking_policy
```

核心映射：

| 输入状态 | 默认策略 |
| --- | --- |
| plan_from_scratch + none 或 rough_notes | Discovery Grill，重新构建 ProposalBrief |
| refine_outline + outline | Refinement Grill，保留主结构并补逻辑缺口 |
| polish_existing + full_draft | 跳过规划前 Grill 或只执行最小事实确认，后续进入 Claim Extraction |
| quick | 只解决阻断性缺口，允许记录低风险 Assumption |
| standard | 解决必填和核心逻辑缺口，启用规则与事实核验 |
| deep | 允许更多高价值问题，启用跨章节核验和 Review Grill |

- [ ] WorkPolicy 由确定性规则优先生成，模糊情况才调用结构化 LLM
- [ ] 用户不直接编辑 max_questions、Reviewer 列表等底层参数，只确认系统工作方案
- [ ] 有规则包、有思路不代表问题一定少，deep 模式可以进入 Refinement Grill
- [ ] 无规则包、无思路不代表必须深度追问，quick 模式只完成最低阻断信息
- [ ] WorkPolicy 必须版本化，并在后续 Planner、Writer、Reviewer 和 Graph 中作为只读输入

处理方式：新增策略生成器。

##### 7-3 建立 Skill 分层和改编边界

内部结构：

```text
grant_grill_router
→ grant_grilling_policy
→ grant_grill_service
```

职责：

| 模块 | 职责 |
| --- | --- |
| grant_grill_router | 根据 IntakeProfile、WorkPolicy 和缺口选择 discovery、refinement、skip 或 evidence_confirmation |
| grant_grilling_policy | 选择下一个问题、展开依赖、生成推荐答案并判断完成 |
| grant_grill_service | 负责权限、会话、持久化、幂等、接口和恢复 |

- [ ] Router、Policy 和 Service 使用固定 Structured Output
- [ ] Policy 不直接操作 ORM，只接收明确输入并返回决策
- [ ] 三种 Profile 不复制三套 Service
- [ ] Skill Prompt 单独版本化，可替换模型但不改变数据库协议
- [ ] 若复制或改编仓库文本，保留来源记录和 MIT License 说明

处理方式：重写现有 Grill Me 的职责分层，复用会话和权限基础。

##### 7-4 建立先检索、后追问的缺口分类

每个候选问题必须先分类：

```text
rule_fact
user_fact
planning_decision
conflict_confirmation
review_decision
```

处理顺序：

```text
rule_fact
→ Rule RAG

user_fact
→ User Evidence RAG

planning_decision
→ Planning Grill

conflict_confirmation
→ Human Confirmation

review_decision
→ 阶段 9 Review Grill
```

- [ ] 可由 Rule RAG 明确回答的章节、字数、资格和预算事实不得再次询问用户
- [ ] 可由 User Evidence RAG 明确回答的论文、项目、实验和数值事实不得再次询问用户
- [ ] 材料只证明事实存在但不能决定是否作为主线时，仍需 Grill 用户作出选择
- [ ] 自动解决结果保存 source、evidence_id、confidence 和 auto_resolved 状态
- [ ] 证据冲突、主体归属不清和关键数值不一致不得自动关闭问题

处理方式：新增问题分类与自动消解逻辑，复用阶段 6 双检索 Service。

##### 7-5 建立 Discovery、Refinement 和 Review Profile

Profile 只保存模式特有的决策主题、优先级和完成条件，共用同一核心 Service。

Discovery Profile：

- [ ] 研究对象
- [ ] 核心问题
- [ ] 研究目标
- [ ] 研究内容
- [ ] 技术路线
- [ ] 创新方向
- [ ] 前期基础需求
- [ ] 预期成果

Refinement Profile：

- [ ] 规则要求是否覆盖
- [ ] 目标与内容是否对应
- [ ] 研究内容之间是否重复
- [ ] 创新点是否具有实质差异
- [ ] 技术路线是否支撑目标
- [ ] 前期证据是否充分
- [ ] 哪些用户结构必须保留

Review Profile：

- [ ] 在本阶段定义输入、输出、问题类型和完成条件
- [ ] 输入必须为阶段 9 Reviewer 产生的 ReviewIssue、EvidenceGap、RuleGap 或 Conflict
- [ ] 不在本阶段提前执行正文评审或修订

处理方式：新增 Profile 配置，Discovery 与 Refinement 立即启用，Review 延后启用。

##### 7-6 建立决策树式会话数据模型

新增或扩展：

```text
GrillSession
GrillDecisionNode
GrillAnswer
ProposalDecision
```

GrillDecisionNode 至少包含：

```text
node_id
session_id
topic
question
question_type
depends_on
blocking
priority
recommended_answer
recommendation_reason
rule_evidence_ids
user_evidence_ids
affected_sections
status
```

- [ ] 每次只向用户展示一个当前最高优先级问题
- [ ] 用户回答后可以创建、关闭、替换或标记后续节点不适用
- [ ] 同一已确认决策不得重复询问
- [ ] 支持采用推荐、修改后采用、自定义、跳过和提前结束
- [ ] 推荐答案不得在用户确认前写入 ProposalBrief 或正文
- [ ] 相同 answer 幂等重试不得重复创建 ProposalDecision

处理方式：新增结构化决策树模型，迁移现有固定问题记录。

##### 7-7 建立项目共识资产

本阶段正式产出：

```text
ProposalBrief
ProposalDecisionLedger
GrantGlossary
AssumptionRegister
```

ProposalBrief 至少包含：

```text
research_object
core_problem
research_goal
research_contents
methodology
innovation_focus
existing_foundation
expected_outputs
scope_exclusions
```

- [ ] 每个已确认答案即时更新 DecisionLedger
- [ ] ProposalBrief 只读取 confirmed ProposalDecision
- [ ] GrantGlossary 保存规则包术语和用户最终采用术语
- [ ] quick 模式允许低风险 Assumption 进入 AssumptionRegister
- [ ] deep 模式的高风险 Assumption 必须在进入 Writer 前消除或人工豁免
- [ ] Planner 不再重新解析完整 Grill 对话，只读取确认后的 ProposalBrief
- [ ] 修改已确认决策时保存版本，并计算可能受影响的后续章节和 Claim

处理方式：新增项目共识模型。

##### 7-8 重写完成条件和降级策略

移除固定五轮和固定三轮作为主要完成条件，改为：

```text
阻断决策是否清零
+ ProposalBrief 必填字段是否完整
+ 当前 quality_level 是否允许剩余缺口
+ 用户是否确认当前共识
+ 是否达到 max_questions 安全上限
```

结束方式：

```text
completed
question_budget_reached
user_ended
skipped
blocked_missing_rule
blocked_missing_evidence
```

- [ ] min_questions 和 max_questions 只作为策略参数和安全边界
- [ ] 达到问题上限时输出剩余缺口，不得伪装完成
- [ ] quick 模式允许带低风险假设继续
- [ ] standard 模式存在高风险缺口时必须二次确认
- [ ] deep 模式存在阻断缺口时不得直接进入 Writer
- [ ] 每次结束必须保存 completion_reason

处理方式：重写现有 Grill 停止逻辑。

##### 7-9 冻结前端和后续阶段接口协议

本阶段不完成正式前端，但必须冻结阶段 12 可直接消费的协议：

```text
WorkPlanPreview
QuestionCard
ConsensusSummary
```

QuestionCard 至少包含：

```text
question
recommended_answer
recommendation_reason
linked_rule_sources
linked_user_evidence
affected_sections
blocking
available_actions
```

- [ ] 项目创建接口接收 task_mode、quality_level 和材料引用
- [ ] Intake 接口返回系统检测结果和 WorkPlanPreview
- [ ] Grill 接口每次只返回一个 QuestionCard
- [ ] 共识接口返回 ProposalBrief、DecisionLedger、AssumptionRegister 和剩余缺口
- [ ] 正式页面实现延期至阶段 12，阶段 7 只提供 API、Schema 和最小调试入口

处理方式：新增接口契约，不提前进行大规模前端开发。

##### 7-10 建立阶段测试

- [ ] 覆盖至少 10 个 Intake 组合案例，不按八套后端实现
- [ ] 有规则包、有框架、deep 时进入 Refinement Grill 而不是直接跳过
- [ ] 无规则包、无思路、quick 时只询问最低阻断信息并记录 rule_gap
- [ ] 已有完整初稿、polish_existing、quick 时可以跳过规划前 Grill
- [ ] 规则事实和用户事实可以被双 RAG 自动解决
- [ ] 规划决策不能被 RAG 自动替用户决定
- [ ] 问题依赖顺序、推荐答案接受、修改、跳过和提前结束均有测试
- [ ] ProposalBrief、DecisionLedger 和 GrillSession 刷新后可恢复
- [ ] 相同回答重试保持幂等

阶段退出条件：

- ProposalIntakeProfile 和 ProposalWorkPolicy 已落地并版本化
- 用户只选择任务目标和交付深度，不需要选择八种后端模式
- Discovery 与 Refinement Grill 共用 Router、Policy 和 Service
- Rule RAG 与 User Evidence RAG 可以自动消除事实问题
- 每次只询问一个当前最高优先级决策并提供推荐答案和依据
- ProposalBrief、DecisionLedger、Glossary 和 AssumptionRegister 可以持久化恢复
- 固定轮数已替换为问题预算、阻断缺口和用户确认
- 用户确认 ProposalBrief 后才允许进入阶段 8
- Review Grill 协议已冻结，但实际执行明确留在阶段 9
- LangGraph 子图和正式前端明确留在阶段 10 与阶段 12，不在本阶段越界实现

#### 1.2.11 阶段 8：基于项目共识的 Requirement、Claim、Evidence 数据闭环

目标：将阶段 7 已确认的项目共识、规则包和用户资料转化为 Requirement、SectionPlan、ClaimPlan 与 EvidenceNeed。Claim 规划不得直接根据零散对话或原始上传材料自行猜测用户意图。

直接依赖：阶段 7

强制输入：confirmed ProposalBrief、ProposalWorkPolicy、GrantPackVersion、IntakeKnowledgeSnapshot

##### 8-1 定义 Claim 模型

```text
Claim
claim_type
section_id
claim_text
claim_status
source_mode
risk_level
proposal_decision_ids
```

Claim 类型至少包含：

- [ ] factual
- [ ] numerical
- [ ] interpretive
- [ ] proposed
- [ ] comparative
- [ ] eligibility
- [ ] unsupported

- [ ] 每个 ClaimPlan 可以追溯到 ProposalBrief 字段或 ProposalDecision
- [ ] 未经确认的 Grill 推荐不得转为 Claim

处理方式：新增。

##### 8-2 建立 Requirement 绑定

- [ ] 每个章节计划保存 target_requirement_ids
- [ ] Claim 保存 addressed_requirement_ids
- [ ] 一个 Requirement 可以被多个 Claim 响应
- [ ] 一个 Claim 可以响应多个相邻 Requirement
- [ ] 规则覆盖率从结构化绑定计算，不由模型自由打分
- [ ] 未覆盖 mandatory Requirement 进入 requirement_gap

处理方式：新增。

##### 8-3 建立 User Evidence 绑定

- [ ] factual 和 numerical Claim 必须关联 UserEvidence
- [ ] proposed Claim 可以关联已有基础证据，但必须标记为未来计划
- [ ] interpretive Claim 标记 inference，并保存推断依据
- [ ] ClaimEvidenceBinding 保存 support_type、support_strength 和 reviewer_status
- [ ] EvidenceUsage 继续保存实际注入快照，用于核对 Claim 是否引用了候选外证据

处理方式：新增并复用 EvidenceUsage。

##### 8-4 定义 Claim 生命周期和决策失效传播

```text
planned
→ retrieved
→ drafted
→ verified
→ human_approved
→ locked

异常状态：
missing_evidence
partial_support
conflicted
rejected
outdated
```

- [ ] 锁定 Claim 不允许自动修改事实和数值
- [ ] 规则包版本变化时，受影响 Claim 标记 outdated
- [ ] 用户资料撤销授权时，受影响 Claim 重新进入 missing_evidence
- [ ] Claim 修改后重新执行事实和规则核验
- [ ] ProposalDecision 修改时，关联 ClaimPlan 和 SectionPlan 标记 needs_replan
- [ ] 只重建受影响的章节和 Claim，不清空全部项目状态

处理方式：新增。

##### 8-5 基于 WorkPolicy 改造章节与 Claim 规划器

规划入口：

```text
Confirmed ProposalBrief
+ ProposalWorkPolicy
+ GrantPackVersion
+ Mandatory Requirements
+ User Evidence Inventory
→ Section Plan
→ Claim Plan
→ Requirement Gap
→ Evidence Gap
→ 用户确认
```

规划模式：

```text
create
complete
validate
extract_from_draft
```

- [ ] plan_from_scratch 使用 create
- [ ] refine_outline 使用 complete 或 validate，并根据 preserve_user_structure 保留用户主结构
- [ ] polish_existing + full_draft 优先使用 extract_from_draft，不重新生成整套框架
- [ ] Planner 不再自行创造通用基金章节
- [ ] Section Schema 是章节结构的主要来源
- [ ] Planner 只在规则允许范围内补充子章节
- [ ] 每个 SectionPlan 包含 requirement_ids、claim_intents、evidence_needs、proposal_decision_ids 和 questions
- [ ] 仍缺少关键决策时返回阶段 7，而不是直接编造内容
- [ ] 规划结果确认后才物化 ProposalSection 和 Claim 草案

处理方式：重写 Planner 输入流程，复用 ProposalPlan。

##### 8-6 Claim 规划测试

- [ ] 国自然、省基金和教改项目生成不同 Section Plan
- [ ] Mandatory Requirement 全部进入章节计划
- [ ] 无用户证据时产生 evidence_gap
- [ ] 用户资料充足时生成具体 Claim Intent
- [ ] 错误规则包版本不会复用旧章节计划
- [ ] Planner 不得把规则原文当作用户事实
- [ ] 修改 ProposalDecision 后只失效受影响 Claim
- [ ] full_draft 润色路径不会无故重建用户章节结构

阶段退出条件：

- SectionPlan、Requirement、Claim Intent、ProposalDecision 和 User Evidence Need 已结构化关联
- Mandatory Requirement 覆盖可以自动计算
- 事实性 Claim 在规划阶段即可暴露证据缺口
- 章节规划不再由固定 NSFC Prompt 或零散对话决定
- 所有 ClaimPlan 都能追溯到 confirmed ProposalBrief
- 阶段 7 的核心决策变更可以触发局部重规划

#### 1.2.12 阶段 9：章节生成、双侧 Reviewer、Review Grill 与局部修订

目标：使 Writer、Reviewer 和 Reviser 根据 ProposalWorkPolicy 适配从头生成、续写、润色和定点修改，并将需要用户决策的评审问题交给 Review Grill，而不是为不同用户路径复制 Agent 或后端流程。

直接依赖：阶段 8

##### 9-1 改写 Writer 输入协议和写作模式

新增或扩展字段：

```text
section_plan
claim_plans
rule_context
user_evidence_context
protected_facts
missing_evidence
proposal_brief
work_policy
writing_mode
```

writing_mode：

```text
generate
continue
audit_and_polish
targeted_rewrite
```

- [ ] rule_context 与 user_evidence_context 分区传入 Prompt
- [ ] 规则证据只约束结构、内容和限制
- [ ] 用户证据只支撑事实、数值、经历和能力
- [ ] generate 用于从头生成，continue 用于补全已有初稿，audit_and_polish 用于保留原文后润色，targeted_rewrite 用于局部修订
- [ ] Writer 输出 Claim 列表和 Claim 到句段位置映射
- [ ] factual 或 numerical Claim 无证据时不得以确定语气生成
- [ ] 所有 evidence_id 必须来自本次候选或已批准绑定
- [ ] preserve_user_structure 和 protected_facts 必须被 Writer 强制执行

处理方式：重写 Writer Prompt 和 SectionDraft 版本，复用 write_service。

##### 9-2 建立规则侧核验器

- [ ] Requirement Coverage Checker
- [ ] Section Structure Checker
- [ ] Word Limit Checker
- [ ] Eligibility Rule Checker
- [ ] Budget Compliance Checker
- [ ] Submission Format Checker

- [ ] 每个问题关联 requirement_id 和 source chunk
- [ ] 规则侧核验不得修改用户事实
- [ ] 低风险格式问题可自动修订
- [ ] 资格、预算和冲突规则默认进入人工确认

处理方式：新增 Checker，复用 Reviewer 路由。

##### 9-3 建立用户事实核验器

- [ ] Claim Support Checker
- [ ] Citation Entailment Checker
- [ ] Numerical Consistency Checker
- [ ] Role Attribution Checker
- [ ] Timeline Consistency Checker
- [ ] Unsupported Claim Checker

- [ ] Reviewer 不得通过生成新事实解决证据不足
- [ ] partial_support 优先缩小表述范围
- [ ] missing_evidence 优先要求补充材料
- [ ] 数值冲突进入 human_review

处理方式：新增 Checker，复用 ReviewResult。

##### 9-4 建立跨系统核验器和质量深度策略

- [ ] Rule Claim Alignment Checker
- [ ] Section Evidence Coverage Checker
- [ ] Cross Section Consistency Checker
- [ ] Terminology Consistency Checker
- [ ] Overclaiming Checker

根据 quality_level：

| 交付深度 | 核验与修订策略 |
| --- | --- |
| quick | 只处理阻断性规则、明显事实错误和必要格式问题，最多 0 至 1 次自动修订 |
| standard | 执行规则、事实和章节一致性核验，最多 2 次自动修订 |
| deep | 执行全部 Checker、跨章节传播、Review Grill 和关键 Claim 人工确认，最多 3 次自动修订 |

- [ ] 修改研究目标后，标记受影响章节和 Claim
- [ ] 同一数值在多个章节不一致时生成一个共享问题
- [ ] 同一 Requirement 被重复响应时只提示冗余，不直接删除

处理方式：新增并由 ProposalWorkPolicy 驱动。

##### 9-5 启用 Review Grill

Review Grill 输入：

```text
ReviewIssue
EvidenceGap
RuleGap
Conflict
AffectedClaim
```

只处理不能由自动规则确定的问题：

- [ ] 事实是否属于当前申请人
- [ ] 某个数值是否可以公开或用于本项目
- [ ] 冲突材料以哪一份为准
- [ ] 是否接受缩小某项创新表述
- [ ] 是否保留缺乏直接证据的计划性判断
- [ ] 用户希望在多个可行表达中采用哪一个

执行顺序：

```text
Writer
→ Reviewer
→ 自动可修复问题进入 Reviser
→ 需要用户决策的问题进入 Review Grill
→ 用户确认修订策略
→ Reviser
→ Reviewer 再核验
```

- [ ] Review Grill 复用阶段 7 的 Router、Policy、Service、QuestionCard 和 DecisionLedger
- [ ] Review Profile 不重新询问已经在 ProposalBrief 中确认的问题
- [ ] Review Decision 必须关联 ReviewIssue 和受影响 Claim
- [ ] quick 模式默认跳过非阻断 Review Grill
- [ ] deep 模式的高风险 ReviewIssue 未解决时不得最终锁定

处理方式：启用阶段 7 已冻结的 Review Profile。

##### 9-6 局部修订闭环

```text
Review Issue 或 Review Decision
→ 定位 Claim
→ 补充检索、缩小表述或采用用户决策
→ 局部重写
→ 再核验
→ 通过或人工确认
```

- [ ] 不重新生成整章
- [ ] 最大自动修订次数由 WorkPolicy 配置
- [ ] 锁定 Claim、ProposalDecision 和人工确认事实不得被覆盖
- [ ] 每次修订保存 diff、触发原因、决策和使用证据
- [ ] 超出上限进入 HumanNode

处理方式：扩展现有 revise_service 和 Reviewer 路由。

阶段退出条件：

- Writer 可以根据 task_mode 选择生成、续写、润色或定点修改
- Writer 的规则上下文和事实上下文可在运行轨迹中独立查看
- Reviewer 可以分别输出规则问题、事实问题和跨系统问题
- Review Grill 只处理真实用户决策，不重复询问可检索事实和已确认共识
- 无依据 Claim 不会被自动伪造证据
- 局部修订不破坏人工锁定内容和 ProposalDecision
- quick、standard、deep 使用同一后端服务但具有不同完成深度

#### 1.2.13 阶段 10：Tool、MCP、LangGraph 与 Multi Agent 适配

目标：将双 RAG、Intake、Planning Grill、Review Grill 和 Claim 闭环接入现有工具和状态图，不重建运行框架，不新增 Agent。

直接依赖：阶段 7、阶段 8、阶段 9

##### 10-1 调整 Tool 与内部 Service 列表

新增或替换：

```text
search_grant_rules
search_user_evidence
get_grant_pack_schema
get_requirement_source
get_user_evidence_source
get_proposal_brief
get_work_policy
validate_rule_coverage
validate_claim_support
save_claim_binding
```

- [ ] 旧 search_guideline 迁移为 search_grant_rules
- [ ] 旧 search_successful_cases 和 get_team_profile 统一由 search_user_evidence 覆盖或保留为受限快捷工具
- [ ] Grill 的问题选择和答案保存保留为内部 service，不暴露为允许模型任意循环调用的开放工具
- [ ] 工具返回明确 knowledge_domain
- [ ] 所有工具继续使用固定 Schema、幂等键和 ToolInvocation 审计

处理方式：拆分和新增，复用工具执行器。

##### 10-2 MCP 资源扩展

新增受控 Resources：

```text
grantpack://pack_version/metadata
grantpack://requirement/id
grantpack://section_schema/key
evidence://user_evidence/id
evidence://claim/id/bindings
```

- [ ] MCP 只暴露受控资源标识，不接受任意路径
- [ ] 规则包公共资源与组织私有资料使用不同权限
- [ ] MCP 不复制 RAG、Grill 或规划逻辑，只调用内部 service
- [ ] ProposalBrief 和 GrillSession 默认不作为公共 MCP Resource 暴露
- [ ] 返回内容默认限制为必要片段和来源信息

处理方式：复用 MCP Server，新增 Resource 和 Tool。

##### 10-3 扩展 ProposalWorkflowState

新增字段：

```text
intake_profile
work_policy
grant_pack_version_id
proposal_brief_version
proposal_decision_ids
grill_session_id
grill_mode
requirement_ids
rule_evidence_ids
user_evidence_ids
claim_ids
requirement_gaps
evidence_gaps
validation_issues
```

- [ ] State 中规则证据与用户证据分字段保存
- [ ] WorkPolicy 和 confirmed ProposalBrief 作为后续节点只读输入
- [ ] Checkpoint 恢复后不得丢失 GrillSession、ProposalDecision 和 Claim Binding
- [ ] 同一节点重试继续使用既有幂等机制
- [ ] Graph 轨迹记录双检索耗时、Grill 问题数、候选数和 completion_reason

处理方式：扩展现有 State。

##### 10-4 建立 Intake 与 Grill 子图

主图入口：

```text
IntakeAssessment
→ RuleResolution
→ ContentAssessment
→ WorkPolicyGeneration
→ GrillRouting
```

规划前 Grill 子图：

```text
LOAD_SESSION
→ RESOLVE_FACTS
→ SELECT_NEXT_DECISION
→ ASK_USER
→ INTERRUPT
```

恢复后：

```text
COMMAND_RESUME
→ SAVE_ANSWER
→ UPDATE_DECISION_TREE
→ CHECK_COMPLETION
→ CONFIRM_PROPOSAL_BRIEF
```

后续路由：

```text
skip
→ Claim Planning 或 Claim Extraction

discovery
→ Discovery Grill
→ Confirmed ProposalBrief
→ Claim Planning

refinement
→ Refinement Grill
→ Confirmed ProposalBrief
→ Claim Planning

review
→ Reviewer
→ Review Grill
→ Reviser
```

- [ ] 使用现有 Checkpointer 和 interrupt、Command 恢复能力
- [ ] 每个问题只产生一次可恢复中断
- [ ] 服务重启后可以从当前问题恢复
- [ ] 相同 answer 重试不重复创建决策
- [ ] Grill 子图只调用阶段 7 service，不复制策略逻辑

处理方式：扩展现有 LangGraph。

##### 10-5 保持三 Agent 边界

Planner Agent：

- [ ] 读取 Confirmed ProposalBrief、GrantPack、Section Schema 和证据库存摘要
- [ ] 输出 ProposalPlan 和 Claim Intent
- [ ] 不参与 Grill 决策，不写正文，不修改 User Evidence

Writer Agent：

- [ ] 使用规则检索、用户证据检索和 confirmed WorkPolicy
- [ ] 输出 SectionDraft 与 Claim 映射
- [ ] 不修改 Requirement、ProposalDecision 和 EvidenceFact

Reviewer Agent：

- [ ] 使用规则核验、事实核验和跨系统核验工具
- [ ] 输出结构化 ReviewResult 和 ReviewIssue
- [ ] 不直接替用户解决 ReviewDecision，不创造新事实，不发布规则包

- [ ] Grill 是工作流能力，不新增第四个 Grill Agent
- [ ] Supervisor 继续使用确定性路由，不允许 Agent 自由协商绕过 WorkPolicy

处理方式：改写工具白名单和 Agent Prompt，不新增 Agent。

##### 10-6 权限和恢复测试

- [ ] Planner 不能发布规则包或修改 ProposalDecision
- [ ] Writer 不能修改 Requirement 或访问其他 Proposal 的用户私有资料
- [ ] Reviewer 不能创建 EvidenceFact 或直接确认 ReviewDecision
- [ ] 公共规则包不能读取组织私有补充包
- [ ] 组织补充包不能覆盖更高优先级官方规则且不留记录
- [ ] GrillSession 跨组织访问被拒绝
- [ ] Checkpoint 恢复后当前问题、答案和 ProposalBrief 版本一致
- [ ] 同一人工回答重复提交被拒绝或幂等回放

阶段退出条件：

- 现有 LangGraph、HITL 和 Multi Agent 测试继续通过
- Intake 与 Grill 子图可以中断、恢复并保持幂等
- 新工具全部有权限、幂等和跨工作区测试
- MCP 与内部 service 使用相同 Schema
- 三 Agent 边界更清晰但 Agent 数量没有增加
- 八种表面用户组合均由 State 和条件边组合，不存在八张 Graph 或八套后端

#### 1.2.14 阶段 11：双 RAG、Grill 与生成闭环 Eval

目标：证明在不更换 Embedding 的情况下，双域架构、自适应 Intake、依赖式 Grill 和 Claim 级核验真实改善系统，并且不会通过增加无效问题换取表面完整度。

直接依赖：阶段 5A、阶段 5B、阶段 9、阶段 10、现有 Eval 框架

##### 11-1 拆分现有评测集

- [ ] 对现有 150 道合成题和 15 道人工锚点重新标注 domain
- [ ] 规则题标记 grant_pack_version_id、requirement_id 和适用范围
- [ ] 用户证据题标记 user_evidence_id、claim_type 和用户角色
- [ ] 混合题同时标记 requirement_id 和 user_evidence_id
- [ ] 无答案题区分 no_applicable_rule 与 missing_evidence
- [ ] 无法可靠标注的旧题不进入正式对比

处理方式：拆分并迁移。

##### 11-2 建立规则 RAG 指标

- [ ] Rule Recall@1、@3、@5
- [ ] Applicable Scope Accuracy
- [ ] Wrong Year Retrieval Rate
- [ ] Wrong Program Retrieval Rate
- [ ] No Applicable Rule Accuracy
- [ ] Rule Citation Traceability
- [ ] Conflict Detection Recall

关键硬约束：

- [ ] 跨规则包泄漏率为 0
- [ ] 错误年度规则进入 Top 5 的比例为 0
- [ ] 返回结果来源可追溯率为 100%

##### 11-3 建立用户证据 RAG 指标

- [ ] Context Recall@1、@3、@5、@20
- [ ] Evidence Precision@5
- [ ] Missing Evidence Accuracy
- [ ] Role Attribution Accuracy
- [ ] Numerical Evidence Accuracy
- [ ] Completed Planned Classification Accuracy
- [ ] Cross Organization Leakage Rate

关键硬约束：

- [ ] 跨组织泄漏率为 0
- [ ] 候选外 evidence_id 引用率为 0
- [ ] 无证据时虚假引用率为 0

##### 11-4 建立 Intake 与 Grill 指标

- [ ] Intake Classification Accuracy
- [ ] Grill Routing Accuracy
- [ ] Blocking Decision Resolution Rate
- [ ] Redundant Question Rate
- [ ] Auto Resolved Fact Rate
- [ ] Recommendation Acceptance Rate
- [ ] Average Questions per Mode
- [ ] ProposalBrief Completion Rate
- [ ] Grill Resume Success Rate
- [ ] Review Grill Issue Resolution Rate

测试场景至少覆盖：

- [ ] 有规则包、有框架、deep
- [ ] 有规则包、无思路、standard
- [ ] 无规则包、无思路、quick
- [ ] 无规则包、有完整初稿、deep
- [ ] 已有完整初稿、只做基础润色
- [ ] 用户提前结束、问题预算耗尽和阻断缺口未解决

关键约束：

- [ ] RAG 已明确回答的问题不得重复询问
- [ ] 规划决策不得被系统自动冒充用户确认
- [ ] deep 模式不得因已有框架而自动跳过高价值 Refinement Grill
- [ ] quick 模式不得因缺少全部材料而无限追问

##### 11-5 建立 Claim 和文档级指标

- [ ] Requirement Coverage
- [ ] Claim Support Rate
- [ ] Unsupported Claim Rate
- [ ] Citation Entailment Accuracy
- [ ] Numerical Error Rate
- [ ] Cross Section Consistency
- [ ] Revision Success Rate
- [ ] Human Acceptance Rate
- [ ] ProposalDecision Preservation Rate

##### 11-6 固定同 Embedding 对比实验

只允许比较：

```text
Baseline A
现有单套 Dense Retrieval + 固定轮次 Grill

方案 B
双 Namespace + Metadata Filter

方案 C
方案 B + BM25 + RRF

方案 D
方案 C + Reranker + Query Rewrite

方案 E
方案 D + 自适应 Intake + Claim Level Grounding
```

明确禁止：

- [ ] 不比较新的 Embedding 模型
- [ ] 不改变向量维度
- [ ] 不用不同 Embedding 掩盖检索策略差异
- [ ] 不把测试 Hash Embedding 结果写入正式简历指标
- [ ] 不只比较问题数量，必须比较阻断问题解决率和冗余问题率

##### 11-7 失败案例报告

- [ ] 错误年度规则
- [ ] 规则和用户资料串库
- [ ] 正确证据未进入候选
- [ ] Reranker 排序错误
- [ ] 用户角色归属错误
- [ ] 数值证据冲突
- [ ] Claim 有引用但不构成支持
- [ ] 已确认 ProposalDecision 被后续 Agent 覆盖
- [ ] Grill 重复询问可检索事实
- [ ] WorkPolicy 路由错误导致过度追问或不足追问
- [ ] Checkpoint 恢复后问题或答案丢失

阶段退出条件：

- 两套 RAG 有独立报告，Grill 有独立报告，并有共同端到端报告
- 所有检索对比使用同一 Embedding
- 改进可以定位到 Metadata、Hybrid、Rerank、Rewrite、Intake、Grill 或 Claim Grounding
- 不同用户起点和交付深度均有冻结测试路径
- 简历指标可以追溯到固定数据集、命令、报告和 run_id

#### 1.2.15 阶段 12：人在回路、前端向导和规则包管理

目标：让用户通过少量可理解的选择进入正确工作路径，并能完成规则包审核、用户资料确认、单问题 Grill、项目共识确认、Review Grill 和 Claim 级人工决策。

直接依赖：阶段 7、阶段 9、阶段 10

##### 12-1 项目创建向导

第一步，选择当前任务：

```text
润色已有文本
完善已有思路
从头规划并起草
```

第二步，选择交付深度：

```text
快速成稿
标准完善
深度打磨
```

第三步，提供材料：

```text
规则材料
├── 选择公共规则包
├── 上传新的规则材料
└── 暂不提供

项目材料
├── 上传框架或初稿
├── 上传论文、项目和实验资料
└── 暂不提供
```

- [ ] 前端不让用户直接选择八种组合模式
- [ ] 创建 Proposal 时保存 task_mode、quality_level 和材料引用
- [ ] 允许上传一组基金要求材料创建自定义规则包草稿
- [ ] 当前 Proposal 保存规则包版本快照
- [ ] 过期或未发布规则包不可直接使用

处理方式：重写 Proposal 创建入口，复用创建接口和上传组件。

##### 12-2 系统评估与工作方案确认页

展示：

```text
规则准备度
内容成熟度
证据准备度
建议 Grill 模式
问题预算范围
是否保留已有框架
Reviewer 深度
最大自动修订次数
```

- [ ] 用户可以修正规则准备度、内容成熟度和证据准备度
- [ ] 用户只能确认或调整高层工作目标，不直接编辑底层 Graph 参数
- [ ] 系统明确说明无正式规则包时无法完成哪些合规核验
- [ ] 用户确认 WorkPlanPreview 后才启动规划前 Grill 或后续流程

处理方式：新增。

##### 12-3 单问题 Planning Grill 界面

每次只显示一个 QuestionCard：

```text
当前问题
系统推荐答案
推荐依据
关联规则
关联用户证据
受影响章节
是否阻断后续流程
```

操作：

```text
采用推荐
修改后采用
自行回答
暂时跳过
结束追问并继续
```

- [ ] 不显示固定的第几题共几题，而显示已解决决策、剩余阻断项和可选优化项
- [ ] 自动解决的规则事实和用户事实可展开查看来源
- [ ] 推荐答案不能绕过用户确认
- [ ] 页面刷新后恢复当前问题和会话状态

处理方式：新增，消费阶段 7 已冻结的 QuestionCard 协议。

##### 12-4 项目共识确认页

展示：

```text
ProposalBrief
ProposalDecisionLedger
GrantGlossary
AssumptionRegister
规则缺口
证据缺口
下一步执行路径
```

- [ ] 用户确认后才进入阶段 8 的 Claim 规划
- [ ] 用户修改关键决策时显示可能受影响的章节
- [ ] quick 模式明确标记保留的假设和未完成核验
- [ ] deep 模式的阻断缺口未解决时禁止直接继续

处理方式：新增。

##### 12-5 规则包审核界面

- [ ] 展示原始文件和分类结果
- [ ] 展示提取的 Requirement、Section Schema 和冲突
- [ ] 支持修改规则类型、适用范围和强制状态
- [ ] 支持确认冲突结论
- [ ] 发布前显示未审核项数量
- [ ] 发布后只读，更新必须创建新版本

处理方式：新增。

##### 12-6 用户资料审核界面

- [ ] 展示提取的 EvidenceFact
- [ ] 用户确认本人角色、时间、数值和可使用范围
- [ ] 支持标记计划、已完成、冲突和拒绝
- [ ] 支持限制某份材料仅用于一个 Proposal
- [ ] 支持撤销授权并显示受影响 Claim

处理方式：新增，复用现有上传和组织权限。

##### 12-7 章节工作区和 Review Grill

证据面板拆为：

```text
规则依据
用户证据
Claim 与核验问题
```

- [ ] 点击规则显示 Requirement、原文、页码和适用范围
- [ ] 点击用户证据显示原始片段、用户角色和验证状态
- [ ] 点击 Claim 显示绑定的 Requirement 和 User Evidence
- [ ] 展示 requirement_gap、evidence_gap、conflict 和 affected_decisions
- [ ] Review Grill 复用单问题交互，但问题卡必须展示 ReviewIssue 和受影响 Claim
- [ ] 人工批准可以锁定 Claim、数值或 ReviewDecision

处理方式：拆分现有证据面板并新增 Review Grill 入口。

##### 12-8 人工决策

- [ ] 接受或驳回 Requirement 提取
- [ ] 接受或驳回 EvidenceFact
- [ ] 替换 Claim 证据
- [ ] 缩小 Claim 表述
- [ ] 锁定数字和事实
- [ ] 确认规则冲突处理
- [ ] 修改 ProposalDecision
- [ ] 修改后重新触发局部规划或核验

阶段退出条件：

- 用户只通过任务目标和交付深度进入系统，不需要理解八种后端路径
- 系统评估、WorkPolicy 和 Grill 模式对用户透明可解释
- 规划前 Grill、共识确认和 Review Grill 均可刷新恢复
- 用户可以从规则包选择开始进入章节规划
- 规则包和用户资料的人工审核入口明确分开
- 前端可以显示 Claim 级双侧证据和 ProposalDecision

#### 1.2.16 阶段 13：求职演示与交付收敛

目标：将双 RAG、规则包、自适应 Intake、项目共识和 Claim 级证据闭环整理为可演示、可复现和可追问的核心项目能力。

直接依赖：阶段 11、阶段 12

##### 13-1 演示数据

- [ ] 一个国自然青年规则包
- [ ] 一个省自然科学基金规则包
- [ ] 一个教改规则包
- [ ] 一组匿名用户论文、项目、实验和团队资料
- [ ] 一个存在证据缺口的案例
- [ ] 一个存在规则冲突的案例
- [ ] 一个存在数值冲突的案例
- [ ] 一个有规则包、有框架、deep 的 Refinement Grill 案例
- [ ] 一个无规则包、无思路、quick 的快速成稿案例
- [ ] 一个已有完整初稿、只做润色的跳过规划前 Grill 案例

##### 13-2 演示主线

```text
选择任务目标和交付深度
→ 选择或上传规则包与项目资料
→ 查看系统 Intake 评估和 WorkPolicy
→ 通过 Planning Grill 确认 ProposalBrief
→ 查看自动提取的章节和要求
→ 生成 Requirement、Claim、Evidence 关系
→ Writer 生成或润色带证据草稿
→ Reviewer 发现缺口
→ 必要时进入 Review Grill
→ 局部修订并人工锁定事实
→ 定稿和导出
```

##### 13-3 必须准备的架构图

- [ ] 现有单套 RAG 与双 RAG 对比图
- [ ] 规则包编译器流程图
- [ ] IntakeProfile、WorkPolicy 与 Grill Router 图
- [ ] Planning Grill、ProposalBrief 和 Claim Planner 依赖图
- [ ] Requirement、Claim、User Evidence 关系图
- [ ] Planner、Writer、Reviewer 和 Grill 工作流边界图
- [ ] 同 Embedding 对比实验结果图

##### 13-4 简历可用指标

优先选择：

- [ ] Wrong Year Retrieval Rate
- [ ] Rule Recall@5
- [ ] User Evidence Recall@5 或 Recall@20
- [ ] Blocking Decision Resolution Rate
- [ ] Redundant Question Rate
- [ ] ProposalBrief Completion Rate
- [ ] Claim Support Rate
- [ ] Unsupported Claim Rate
- [ ] Requirement Coverage
- [ ] Cross Organization Leakage Rate
- [ ] Human Acceptance Rate

所有指标必须来自真实冻结报告，不写目标值和模拟值。

阶段退出条件：

- 新环境可按 README 运行三类规则包和三类用户起点 Demo
- 五分钟内可展示 Intake、Planning Grill、规则包、双 RAG、Claim 绑定和 Reviewer 回退
- 能解释为什么没有为八种组合建立八套后端或八张 Graph
- 每个技术名词均可定位到模型、service、tool、Graph Node、测试和报告
- 简历不宣称更换或训练 Embedding，只强调同模型下的检索系统和自适应工作流优化


#### 1.2.17 全部配置变更总表

| 配置或模块 | 复用 | 拆分 | 新增 | 重写 | 最终处理 |
| --- | :---: | :---: | :---: | :---: | --- |
| bge-base-zh-v1.5 | 是 | 否 | 否 | 否 | 固定为正式 Embedding |
| Hash Embedding | 是 | 否 | 否 | 否 | 仅测试使用 |
| Embedding Provider 选型 | 否 | 否 | 否 | 否 | 取消任务 |
| PDF Parser | 是 | 否 | 否 | 否 | 共享底层解析 |
| MCP 文件读取 | 是 | 否 | 否 | 否 | 共享受控文件边界 |
| AIResource | 是 | 否 | 是 | 否 | 增加知识域和规则包字段 |
| AIChunk | 是 | 是 | 是 | 否 | 同表扩展，分两套 Chunk 配置 |
| Chunker | 部分 | 是 | 否 | 是 | 底层复用，领域策略重写 |
| 向量数据库客户端 | 是 | 否 | 否 | 否 | 共用基础设施 |
| 向量 Namespace | 否 | 是 | 是 | 否 | grant_rule 与 user_evidence |
| RetrievalService | 部分 | 是 | 是 | 是 | 公共基类加两套领域 Service |
| Dense Retrieval | 是 | 是 | 否 | 否 | 两侧独立参数 |
| BM25 | 否 | 是 | 是 | 否 | 两侧独立索引和权重 |
| RRF | 否 | 是 | 是 | 否 | 两侧独立配置 |
| Reranker | 可共用模型 | 是 | 是 | 否 | 评分特征拆分 |
| Query Rewrite | 否 | 是 | 是 | 否 | 规则和事实两套 Prompt |
| Parent Child Retrieval | 否 | 否 | 是 | 否 | 主要用于用户资料侧 |
| Context Prefix | 否 | 是 | 是 | 否 | 两域不同模板 |
| EvidenceUsage | 是 | 否 | 是 | 是 | 改为注入审计 |
| GrantRequirement | 否 | 否 | 是 | 否 | 新增规则语义模型 |
| UserEvidence | 否 | 否 | 是 | 否 | 新增事实证据模型 |
| Claim | 否 | 否 | 是 | 否 | 新增生成语义模型 |
| ClaimEvidenceBinding | 否 | 否 | 是 | 否 | 新增双侧绑定 |
| ProposalIntakeProfile | 否 | 否 | 是 | 否 | 保存规则、内容、证据准备度与用户任务目标 |
| ProposalWorkPolicy | 否 | 否 | 是 | 否 | 将用户起点和交付深度转成统一执行策略 |
| GrillSession | 是 | 否 | 是 | 是 | 复用会话基础，改为可恢复的决策树会话 |
| GrillDecisionNode | 否 | 否 | 是 | 否 | 保存问题依赖、优先级、推荐和来源 |
| ProposalBrief | 否 | 否 | 是 | 否 | 作为 Claim Planner 的唯一用户共识输入 |
| ProposalDecisionLedger | 否 | 否 | 是 | 否 | 保存已确认选择、依据和影响范围 |
| GrantGlossary | 否 | 否 | 是 | 否 | 固定规则术语与用户采用术语 |
| AssumptionRegister | 否 | 否 | 是 | 否 | 管理快速成稿中的未确认假设 |
| ProposalPlan | 是 | 否 | 是 | 是 | 改为读取 confirmed ProposalBrief、WorkPolicy 和规则包 |
| SectionDraft | 是 | 否 | 是 | 是 | 增加写作模式、Claim 映射、双上下文和保护事实 |
| ReviewResult | 是 | 是 | 是 | 是 | 拆成规则、事实、跨系统问题，并输出 Review Grill 输入 |
| search_service | 部分 | 是 | 是 | 是 | 拆为两个检索 service |
| Tool Registry | 是 | 是 | 是 | 否 | 增加双域工具和权限 |
| LangGraph | 是 | 否 | 是 | 否 | 扩展 State，并增加可中断恢复的 Grill 子图 |
| Planner Agent | 是 | 否 | 是 | 是 | 以规则包驱动章节规划 |
| Writer Agent | 是 | 否 | 是 | 是 | 区分规则和用户证据 |
| Reviewer Agent | 是 | 是 | 是 | 是 | 三类核验器 |
| Grill Me | 是 | 是 | 是 | 是 | 保留会话、持久化和 HITL 基础，重写为 Router、Core、Profile 和决策树式追问 |
| 前端创建与证据界面 | 部分 | 是 | 是 | 是 | 增加任务与深度向导、单问题 Grill、共识确认和三个证据视图 |
| Eval | 是 | 是 | 是 | 是 | 两套 RAG、Intake、Grill、Claim 和端到端 Eval |
| Observability | 是 | 是 | 是 | 否 | 分域记录检索耗时和候选 |
| Export | 是 | 否 | 否 | 否 | 继续读取最终批准稿 |

#### 1.2.18 立即执行的第一个开发批次

后续代码修改从以下顺序开始，不得跳步：

1. 冻结当前分支和 dual-rag-baseline 报告
2. 在 AIResource 和 AIChunk 增加 knowledge_domain
3. 建立 grant_rule 与 user_evidence 两个索引 Namespace
4. 对现有 14 份 PDF 和 249 个 Chunk 完成领域分类
5. 将 RetrievalService 拆为公共基类、RuleRAGService 和 UserEvidenceRAGService
6. 保持 bge-base-zh-v1.5 配置不变，并增加 embedding_input_hash
7. 增加生产路径无 domain 检索拒绝测试
8. 建立 GrantProgram、GrantPackVersion、GrantRequirement 和 SectionSchema 最小模型
9. 建立 UserEvidence、EvidenceFact 和授权范围最小模型
10. 将 Writer 的 evidence_context 拆为 rule_context 和 user_evidence_context
11. 新增 search_grant_rules 与 search_user_evidence 工具
12. 从现有评测题中各挑选十道规则题和十道用户证据题，生成第一份双域 baseline

完成以上十二项后，才开始 Hybrid Retrieval 和规则包编译器。阶段 7 不得提前开发，必须等待 Rule RAG、User Evidence RAG、Query Router 和 IntakeKnowledgeSnapshot 稳定。进入阶段 7 后，第二个开发批次按以下顺序执行：

1. 定义 ProposalIntakeProfile 与枚举
2. 定义 ProposalWorkPolicy 和确定性映射规则
3. 将现有 Grill Me 拆为 Router、Policy 和 Service
4. 建立 GrillDecisionNode、GrillAnswer 和 ProposalDecision
5. 接入阶段 6 的 IntakeKnowledgeSnapshot，先自动解决事实问题
6. 实现 Discovery 与 Refinement Profile
7. 建立 ProposalBrief、DecisionLedger、Glossary 和 AssumptionRegister
8. 重写完成条件和降级策略
9. 冻结 WorkPlanPreview、QuestionCard 和 ConsensusSummary 协议
10. 通过阶段 7 固定案例后再进入 Claim 级绑定

#### 1.2.19 明确暂缓和取消事项

本轮明确取消：

- 不尝试新的 Embedding 模型
- 不比较大模型和小模型 Embedding
- 不修改现有向量维度
- 不训练领域 Embedding
- 不做 Embedding 蒸馏和微调

在双 RAG 和 Claim 级闭环稳定前暂缓：

- 不做完整 GraphRAG
- 不做 RAPTOR 全文树索引
- 不替换为 ColBERT 或多向量检索
- 不复现需要训练反思 Token 的 Self RAG
- 不一次收集几十种基金规则包
- 不做公共规则包市场
- 不做完全自动资格裁决
- 不做无人审批的自动发布规则包
- 不扩展更多 Agent
- 不重写现有 LangGraph
- 不在阶段 7 提前创建最终 Claim 或生成正文
- 不在阶段 7 提前实现 Review Grill 的正文问题处理
- 不把 mattpocock/skills 安装成新的 Agent 运行框架
- 不为八种表面组合建立八套后端、八份 Prompt 或八张 Graph
- 不用固定问题数量代替完成条件
- 不继续进行与主链路无关的前端美化
- 不先追求逐句引用，先完成 Claim 级证据绑定
- 不用真实敏感申请材料作为公开 Demo 数据

### 1.3 第 1 步实施记录

> 占位，待执行阶段 1 后补充。

### 1.4 第 2 步实施记录

> 占位，待执行阶段 2 后补充。

### 1.5 第 3 步实施记录

> 占位，待执行阶段 3 后补充。

### 1.6 第 4 步实施记录

> 占位，待执行阶段 4 后补充。

### 1.7 第 5 步实施记录

> 占位，待执行阶段 5A 与阶段 5B 后补充。

### 1.8 第 6 步实施记录

> 占位，待执行阶段 6 后补充。

### 1.9 第 7 步实施记录

> 占位，待完成自适应 Intake、规划前 Grill 与项目共识构建后补充。

### 1.10 第 8 步实施记录

> 占位，待完成基于项目共识的 Requirement、Claim、Evidence 数据闭环后补充。

### 1.11 第 9 步实施记录

> 占位，待完成章节生成、双侧 Reviewer、Review Grill 与局部修订后补充。

### 1.12 第 10 步实施记录

> 占位，待完成 Tool、MCP、LangGraph 与 Multi Agent 适配后补充。

### 1.13 第 11 步实施记录

> 占位，待完成双 RAG、Grill 与生成闭环 Eval 后补充。

### 1.14 第 12 步实施记录

> 占位，待完成人在回路、前端向导和规则包管理后补充。

### 1.15 第 13 步实施记录

> 占位，待完成求职演示与交付收敛后补充。

## 2. 当前架构快照

> 占位，后续根据双 RAG 第一批改造后的真实代码补充。

## 3. 当前关键问题

> 占位，后续根据实施中发现的问题补充。

## 4. 对原工作清单的取舍

> 占位，后续记录保留、拆分、重写和取消的具体代码决策。

## 5. 实施计划与完成记录

> 占位，后续按阶段补充真实修改文件、测试命令和验证结果。

## 6. 里程碑与工期

> 占位，后续根据实际开发速度填写，不预先承诺固定工期。

## 7. 每日执行顺序

> 占位，后续在开始实施时拆分。

## 8. 成功标准

> 占位，最终以阶段 11 的冻结报告和阶段 13 的交付结果为准。

## 9. 范围控制

> 占位，继续以 1.2.19 的暂缓和取消事项为当前约束。

## 10. 优先级

> 占位，当前优先级以 1.2.18 的第一个开发批次为准。

## 11. 持续有效的风险提示

> 占位，后续补充规则版本错误、用户资料泄漏、证据误归属和自动修订覆盖人工事实等风险。

## 12. 一句话路线

> 占位，完成首个双 RAG 里程碑后再根据真实结果更新。
